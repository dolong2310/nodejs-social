# Danh sách operationId tham chiếu
# Các operationId được định nghĩa trong paths.yaml
# Auth: register | login | logout | refreshToken | verifyEmail | resendVerifyEmail | forgotPassword | resetPassword | changePassword
# Users: getMe | updateMe | getUserProfile
# googleLogin | uploadImage | uploadVideo | uploadVideoHLS | getVideoStatus
# getNewFeeds | createPost | getPostDetail | getPostsType
# createBookmark | deleteBookmark | followUser | unfollowUser | search
# createConversation | getConversations
# getStaticImage | getStaticVideoStream | getStaticVideoHLSMaster | getStaticVideoHLSSegment
