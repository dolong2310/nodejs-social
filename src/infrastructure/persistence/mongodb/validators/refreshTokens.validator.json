{
  "$jsonSchema": {
    "title": "refreshTokens object validation (aligned with RefreshTokenEntity + createRefreshToken)",
    "bsonType": "object",
    "required": ["_id", "token", "userId", "createdAt"],
    "properties": {
      "_id": {
        "bsonType": "objectId"
      },
      "token": {
        "bsonType": "string",
        "description": "JWT refresh token string"
      },
      "userId": {
        "bsonType": "objectId"
      },
      "createdAt": {
        "bsonType": "date",
        "description": "Set in RefreshTokenEntity constructor when omitted"
      },
      "exp": {
        "bsonType": "date",
        "description": "Optional. DatabaseService.createRefreshTokensIndex defines TTL on exp; app insert path does not set exp yet — include when you wire TTL in code."
      }
    },
    "additionalProperties": false
  }
}
