{
  "$jsonSchema": {
    "title": "refreshTokens — aligned with modules/auth/infrastructure/persistence/mongo/refresh-token.model",
    "bsonType": "object",
    "required": ["_id", "userId", "token", "expiresAt"],
    "properties": {
      "_id": {
        "bsonType": "string",
        "minLength": 36,
        "maxLength": 128
      },
      "userId": {
        "bsonType": "string",
        "minLength": 36,
        "maxLength": 128
      },
      "token": {
        "bsonType": "string",
        "description": "Refresh token string (JWT)"
      },
      "expiresAt": {
        "bsonType": "date",
        "description": "TTL field; index expireAfterSeconds:0 on expiresAt in database.ts"
      },
      "createdAt": { "bsonType": "date" },
      "updatedAt": { "bsonType": "date" }
    },
    "additionalProperties": false
  }
}
