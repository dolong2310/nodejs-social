{
  "$jsonSchema": {
    "title": "hashtags (post.repository findAndUpsertHashtags $setOnInsert: _id, name, createdAt)",
    "bsonType": "object",
    "required": ["_id", "name", "createdAt"],
    "properties": {
      "_id": { "bsonType": "objectId" },
      "name": { "bsonType": "string" },
      "createdAt": { "bsonType": "date" }
    }
  }
}
