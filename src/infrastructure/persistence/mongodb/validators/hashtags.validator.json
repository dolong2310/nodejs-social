{
  "$jsonSchema": {
    "title": "hashtags — aligned with modules/hashtag/infrastructure/mongo/hashtag.model",
    "bsonType": "object",
    "required": ["_id", "name"],
    "properties": {
      "_id": {
        "bsonType": "string",
        "minLength": 36,
        "maxLength": 128
      },
      "name": {
        "bsonType": "string",
        "minLength": 1
      },
      "createdAt": { "bsonType": "date" },
      "updatedAt": { "bsonType": "date" }
    },
    "additionalProperties": false
  }
}
