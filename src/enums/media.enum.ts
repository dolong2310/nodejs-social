export enum MediaType {
  IMAGE = 'image',
  VIDEO = 'video',
  VIDEO_HLS = 'video-hls'
}

export enum EncodingVideoStatus {
  PENDING = 'pending',
  PROCESSING = 'processing',
  SUCCESS = 'success',
  FAILED = 'failed',
}
