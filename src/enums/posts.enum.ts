export enum PostType {
  POST = 'post',
  REPOST = 'repost',
  COMMENT = 'comment',
  QUOTE = 'quote'
}

export enum PostAudience {
  PUBLIC = 'public',
  FRIENDS = 'friends',
  ONLY_ME = 'only_me'
}
