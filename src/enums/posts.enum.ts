export enum EPostType {
  POST = 'post',
  REPOST = 'repost',
  COMMENT = 'comment',
  QUOTE = 'quote'
}

export enum EPostAudience {
  PUBLIC = 'public',
  FOLLOWERS = 'followers',
  ONLY_ME = 'only_me'
}
