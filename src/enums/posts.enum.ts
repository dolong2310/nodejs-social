export enum PostType {
  POST = 'post',
  REPOST = 'repost',
  COMMENT = 'comment',
  QUOTE = 'quote'
}

export enum PostAudience {
  PUBLIC = 'public',
  FOLLOWERS = 'followers',
  ONLY_ME = 'only_me'
}
