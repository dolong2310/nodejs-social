export enum ESearchPeople {
  FRIENDS = 'friends',
  NOT_FRIENDS = 'not_friends',
  ONLY_ME = 'only_me'
}

export enum ESearchType {
  POST = 'post',
  USER = 'user',
  IMAGE = 'image',
  VIDEO = 'video',
  VIDEO_HLS = 'video-hls'
}
