export enum ESearchPeopleFollow {
  FOLLOWING = 'following',
  NOT_FOLLOWING = 'not_following',
  ONLY_ME = 'only_me'
}

export enum ESearchType {
  POST = 'post',
  USER = 'user',
  IMAGE = 'image',
  VIDEO = 'video',
  VIDEO_HLS = 'video-hls'
}
