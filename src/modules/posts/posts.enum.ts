export enum EPostType {
  POST = 'post',
  REPOST = 'repost',
  COMMENT = 'comment',
  QUOTE = 'quote'
}

export enum EPostAudience {
  PUBLIC = 'public',
  FRIENDS_ONLY = 'friends-only',
  ONLY_ME = 'only-me'
}
