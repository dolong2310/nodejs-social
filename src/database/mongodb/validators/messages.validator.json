{
  "$jsonSchema": {
    "title": "messages / IChatMessage (ChatMessageSchema; chat DB collection `messages`)",
    "bsonType": "object",
    "required": ["_id", "chatId", "senderId", "createdAt"],
    "properties": {
      "_id": { "bsonType": "objectId" },
      "chatId": { "bsonType": "objectId" },
      "senderId": { "bsonType": "objectId" },
      "createdAt": { "bsonType": "date" },
      "text": {
        "bsonType": "string",
        "description": "Optional; API requires non-empty text and/or attachments (CHAT_MESSAGE_EMPTY)"
      },
      "attachments": {
        "bsonType": "array",
        "items": {
          "bsonType": "object",
          "required": ["key", "mime", "size"],
          "properties": {
            "key": { "bsonType": "string" },
            "mime": { "bsonType": "string" },
            "size": {
              "bsonType": ["int", "long", "double"],
              "description": "IChatAttachment.size (bytes)"
            },
            "url": { "bsonType": "string" }
          },
          "additionalProperties": false
        }
      }
    },
    "additionalProperties": false
  }
}
