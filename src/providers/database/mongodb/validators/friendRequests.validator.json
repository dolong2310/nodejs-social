{
  "$jsonSchema": {
    "title": "friendRequests (FriendRequestSchema + friendRequest.repository)",
    "bsonType": "object",
    "required": ["_id", "fromUserId", "toUserId", "createdAt"],
    "properties": {
      "_id": { "bsonType": "objectId" },
      "fromUserId": { "bsonType": "objectId" },
      "toUserId": { "bsonType": "objectId" },
      "createdAt": { "bsonType": "date" }
    },
    "additionalProperties": false
  }
}
