---
phase: 05-notifications-inbox
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/container/index.ts
  - src/app.ts
  - src/routes/notifications.route.ts
  - src/controllers/notifications.controller.ts
  - src/validations/notifications.validation.ts
  - src/dtos/requests/notification.request.dto.ts
  - src/dtos/responses/notification.response.dto.ts
  - swagger/paths.yaml
  - swagger/components.yaml
autonomous: true
requirements:
  - NOTF-01
  - NOTF-02

must_haves:
  truths:
    - `GET /api/notifications` (hoặc prefix `config.api.prefix` + `/notifications`) có `protect` + verified user, query `limit`, `cursor`, `unreadOnly` (boolean string hoặc query parser thống nhất project).
    - `PATCH .../read` batch/all + `PATCH .../:notificationId/read` single — khớp 05-CONTEXT D-03.
    - OpenAPI mô tả response **flat array** + `nextCursor`; schema **theo type** (oneOf / separate schemas).
  artifacts:
    - path: src/routes/notifications.route.ts
      provides: mount routes
    - path: swagger/paths.yaml
      provides: paths notifications
  key_links:
    - from: NotificationsService
      to: HTTP
      via: NotificationsController
---

## Objective

Expose **REST inbox** khớp NOTF-01 + contract NOTF-02; đăng ký DI container và Swagger.

## Scope

- In: controller, route, validation (express-validator hoặc pattern hiện có), DTO request/response, `Container`, `setupRoutes` trong `app.ts`, swagger paths/components.
- Out: Gọi create từ friends/chat (plan 03).

## Response list item (gợi ý executor)

- Trả về `_id`, `read`, `createdAt`, `type`, `actor`, `payload` (đúng shape type), field `title` hoặc `summary` **ngắn** generic theo type (D-05) nếu muốn tách khỏi FE — hoặc FE dựa `type` + payload; plan cho phép một trong hai miễn OpenAPI rõ.

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-notifications-inbox/05-CONTEXT.md
@src/routes/friends.route.ts
@src/controllers/friends.controller.ts
@src/container/index.ts
@swagger/paths.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: DTO + validation + controller + route + container</name>
  <files>src/dtos/requests/notification.request.dto.ts, src/dtos/responses/notification.response.dto.ts, src/validations/notifications.validation.ts, src/controllers/notifications.controller.ts, src/routes/notifications.route.ts, src/container/index.ts</files>
  <read_first>src/controllers/chatMessages.controller.ts, src/routes/chats.route.ts, src/container/index.ts, src/services/notifications.service.ts</read_first>
  <action>
    - DTO: query `limit` (1–100 default ví dụ 20), optional `cursor`, optional `unreadOnly`. Body mark batch: optional array `ids` — empty/absent = all.
    - Controller methods: `list`, `markRead`, `markOneRead`.
    - Route class `NotificationsRoute` pattern `BaseRoute`; đăng ký `app.use(..., notificationsRouter())` tại `/notifications` dưới cùng prefix API như các router khác.
    - Container: `getNotificationsService()`, `getNotificationsController()`, `getNotificationsValidation()`; khởi tạo sau khi `NotificationsService` có trong container (wire repository + user + block; **chưa** socket nếu service chưa có — plan 03 bổ sung tham constructor).
  </action>
  <verify>
    <automated>cd /Users/dolong/Documents/NodeJS/nodejs-social && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - `grep -r "notifications" src/app.ts src/routes/notifications.route.ts` có import/mount rõ ràng.
    - `container/index.ts` chứa `getNotificationsService` hoặc tên getter thống nhất với controller.
  </acceptance_criteria>
  <done>REST layer wired.</done>
</task>

<task type="auto">
  <name>Task 2: Swagger OpenAPI cho notifications</name>
  <files>swagger/paths.yaml, swagger/components.yaml, swagger/tags.yaml (nếu cần tag mới)</files>
  <read_first>swagger/paths.yaml, swagger/components.yaml, src/dtos/responses/notification.response.dto.ts</read_first>
  <action>
    Thêm tag `Notifications` (nếu chưa có). Định nghĩa paths `GET /notifications`, `PATCH /notifications/read`, `PATCH /notifications/{notificationId}/read` với security bearer. Components: `NotificationItem` với `discriminator.propertyName: type` + `oneOf` cho payload (hoặc 4 schema rõ tên) khớp D-08.
  </action>
  <verify>
    <automated>cd /Users/dolong/Documents/NodeJS/nodejs-social && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "/notifications" swagger/paths.yaml`.
    - `grep -q "Notification" swagger/components.yaml`.
  </acceptance_criteria>
  <done>Contract documented.</done>
</task>

</tasks>

## Done when

Client có thể gọi list/mark-read qua REST và Swagger hiển thị đủ type/payload.
