---
phase: 05-notifications-inbox
plan: "03"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - src/services/socket.service.ts
  - src/services/notifications.service.ts
  - src/container/index.ts
  - src/services/friends.service.ts
  - src/services/chatMessages.service.ts
  - src/services/chats.service.ts
  - src/app.ts
autonomous: true
requirements:
  - NOTF-02
  - NOTF-03

must_haves:
  truths:
    - Sau mỗi `insert` notification thành công, server **emit** Socket.IO event **`notification:new`** tới socket của `recipientId` nếu online (map hiện có trong `SocketService`).
    - **Không** emit nếu không có row DB (persist-then-emit — NOTF-03).
    - `friend_request`: sau `friendRequestRepository.insertPendingRequest` thành công trong `sendFriendRequest` → notify **toUserId**; không chạy khi throw block (đã có sẵn).
    - `friend_accepted`: sau khi accept tạo friendship thành công → notify **fromUserId** (người đã gửi lời mời), actor = accepter.
    - `new_message`: sau `insertMessage` — direct: recipient = peer; group: mọi member khác sender; skip recipient nếu `isBlockedEitherWay(recipient, sender)` (D-13).
    - `added_to_group`: sau `inviteMember` insert member → notify **invitee**, actor = inviter, payload có `conversationId`.
  artifacts:
    - path: src/services/socket.service.ts
      provides: `emitNotificationToUser(userId: string, payload: object): void` (public)
    - path: src/services/notifications.service.ts
      provides: gọi emit sau create
  key_links:
    - from: FriendsService / ChatMessagesService / ChatsService
      to: NotificationsService
      via: DI
---

## Objective

Wire **hooks** tạo notification từ graph + chat; **Socket emit** sau persist; giữ **block** đồng bộ D-13–D-15.

## Scope

- In: `SocketService` public emit; `NotificationsService` nhận `SocketService` (hoặc interface tối thiểu) và gọi sau mỗi create; inject `NotificationsService` vào `FriendsService`, `ChatMessagesService`, `ChatsService`; cập nhật `Container` thứ tự khởi tạo; `createApp` nếu cần truyền socket vào container (tránh duplicate SocketService — **một** instance: hiện tạo trong `createApp` — refactor nhỏ: lấy socket từ factory hoặc set `container.setSocketService` sau khi tạo cả hai; executor chọn pattern ít đụng nhất, miễn một `SocketService` dùng chung).
- Out: Phase 6 room/join đầy đủ; email/push.

## Gợi ý refactor bootstrap (executor chọn một)

- **A:** `Container` nhận optional `socketService` trong `getOrSet`; `createApp` tạo `SocketService` trước, `Container.getOrSet(db, redis, socket)`.
- **B:** `NotificationsService` lấy emitter qua setter `setNotificationEmitter(fn)` gọi sau `socket.run()` trong `createApp`.

Miễn acceptance dưới đây thỏa.

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-notifications-inbox/05-CONTEXT.md
@src/services/socket.service.ts
@src/services/friends.service.ts
@src/services/chatMessages.service.ts
@src/services/chats.service.ts
@src/app.ts
@src/container/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: SocketService emit + NotificationsService persist-then-emit</name>
  <files>src/services/socket.service.ts, src/services/notifications.service.ts, src/container/index.ts, src/app.ts</files>
  <read_first>src/services/socket.service.ts, src/services/notifications.service.ts, src/container/index.ts, src/app.ts</read_first>
  <action>
    - Thêm method public trên `SocketService`: ví dụ `emitToUser(userId: string, event: string, data: unknown)` — lookup `this.users.get(userId)`, nếu có thì `this.io.to(socketId).emit(event, data)`.
    - `NotificationsService`: sau mỗi lần insert notification thành công (và sau trim), gọi `socketService.emitToUser(recipientIdHex, 'notification:new', { notification: &lt;shape giống REST item&gt; })`.
    - Wire DI: `Container` cung cấp `SocketService` cho `NotificationsService` — đảm bảo **cùng instance** với `socket.run()` trong `createApp` (refactor `getOrSet` nếu cần).
  </action>
  <verify>
    <automated>cd /Users/dolong/Documents/NodeJS/nodejs-social && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - `grep -q "notification:new" src/services/socket.service.ts src/services/notifications.service.ts` (event string xuất hiện ít nhất trong notifications.service khi emit).
    - `grep -q "emitToUser\\|emitNotificationToUser" src/services/socket.service.ts`.
  </acceptance_criteria>
  <done>NOTF-03 wired at service layer.</done>
</task>

<task type="auto">
  <name>Task 2: Hooks FriendsService + ChatMessagesService + ChatsService</name>
  <files>src/services/friends.service.ts, src/services/chatMessages.service.ts, src/services/chats.service.ts, src/container/index.ts</files>
  <read_first>src/services/friends.service.ts, src/services/chatMessages.service.ts, src/services/chats.service.ts, src/repositories/chatMember.repository.ts, src/models/schemas/chat.schema.ts</read_first>
  <action>
    - **FriendsService** constructor: thêm `INotificationsService`. Trong `sendFriendRequest` sau khi `created` thành công: gọi `notificationsService.notifyFriendRequest({ recipientId: toUserId, actorId: myUserId, requestId: created._id })` hoặc API tương đương đã định nghĩa ở plan 01.
    - Trong `acceptIncomingRequest` sau khi friendship + cleanup request thành công: notify `fromUserId` kiểu `friend_accepted`, actor = `myUserId`.
    - **ChatMessagesService**: inject `INotificationsService`. Sau `insertMessage` và `touchUpdatedAt`, với `chat` loaded: nếu `direct` → một notify cho peer (nếu không blocked với sender); nếu `group` → `listMembers`, mỗi `userId !== sender` và không blocked → một notify; payload `new_message` theo D-06 (text thuần vs chỉ attachment label tiếng Việt hoặc key i18n — dùng string cố định server ví dụ `Đã gửi một ảnh` khi chỉ ảnh).
    - **ChatsService.inviteMember**: sau `insertMember` thành công, gọi `added_to_group` cho `body.userId`, actor = inviter `userId`.
    - Cập nhật mọi chỗ `new FriendsService(` / `new ChatMessagesService(` / `new ChatsService(` trong `container/index.ts` với tham số mới.
  </action>
  <verify>
    <automated>cd /Users/dolong/Documents/NodeJS/nodejs-social && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - `grep -q "NotificationsService\\|INotificationsService" src/services/friends.service.ts src/services/chatMessages.service.ts src/services/chats.service.ts`.
    - `friends.service.ts` có lệnh gọi notify sau nhánh tạo request thành công (grep `notify` hoặc `createFriend` trong file).
  </acceptance_criteria>
  <done>All v1 notification types created from product flows.</done>
</task>

</tasks>

## Done when

Mọi type v1 được tạo từ đúng luồng nghiệp vụ; socket emit sau persist; build xanh.
