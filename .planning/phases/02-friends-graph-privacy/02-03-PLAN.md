---
phase: 02-friends-graph-privacy
plan: "03"
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - src/services/friends.service.ts
  - src/controllers/friends.controller.ts
  - src/routes/friends.route.ts
  - src/dtos/requests/friends.request.dto.ts
  - src/dtos/responses/friends.response.dto.ts
  - src/validations/friends.validation.ts
  - src/container/index.ts
  - src/app.ts
autonomous: true
requirements:
  - FRND-01
  - FRND-02
  - FRND-03
  - FRND-04
  - FRND-05
  - FRND-06

must_haves:
  truths:
    - Authenticated user can send, accept, decline, and revoke friend requests under D-06, D-07 (UTC calendar day), D-09–D-12, D-11 (no self).
    - Accept creates exactly one normalized friendship row and removes the pending request (D-05).
    - User can list friends and incoming/outgoing pending requests with fields sufficient for a typical client (user id + minimal profile fields via existing user load pattern).
    - User can unfriend; normalized friendship row is removed; friend caches invalidated for both parties.
  artifacts:
    - path: src/routes/friends.route.ts
      provides: HTTP surface under /api/friends (via api prefix)
    - path: src/services/friends.service.ts
      provides: Full graph mutations + read helpers used by controller
  key_links:
    - from: src/routes/friends.route.ts
      to: src/controllers/friends.controller.ts
      via: BaseRoute + protect + appLimiter + usersValidation patterns (per bookmarks.route.ts style)
    - from: src/services/friends.service.ts
      to: src/repositories/block.repository.ts
      via: isBlockedEitherWay before send/accept (D-12)
---

## Objective

Ship **`/api/friends`** (D-02) implementing **FRND-01–FRND-06**: directed requests with unique pending, mutual friendship on accept, decline/revoke as delete, friend lists, **~100 outgoing POST sends per UTC calendar day** (D-07/D-08), and unfriend — mirroring **BaseRoute + Container + protect + validation** from the removed followers module.

## Scope

- In: extend `FriendsService`, new controller/route/DTOs/validation, container + `app.ts` mount `router.use('/friends', friendsRouter())`.
- Out: `/api/blocks`, Swagger (plan `04`), fake-data (plan `04`), post feed block filtering (Phase 3).

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-friends-graph-privacy/02-CONTEXT.md
@.planning/phases/02-friends-graph-privacy/02-RESEARCH.md
@src/routes/bookmarks.route.ts
@src/controllers/bookmarks.controller.ts
@src/services/friends.service.ts
@src/repositories/friendship.repository.ts
@src/repositories/friendRequest.repository.ts
@src/repositories/block.repository.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: DTOs, validation, FriendsRoute skeleton</name>
  <files>src/dtos/requests/friends.request.dto.ts, src/dtos/responses/friends.response.dto.ts, src/validations/friends.validation.ts, src/routes/friends.route.ts, src/controllers/friends.controller.ts</files>
  <read_first>
    .planning/phases/02-friends-graph-privacy/02-CONTEXT.md
    src/routes/bookmarks.route.ts
    src/validations/users.validation.ts
    src/controllers/bookmarks.controller.ts
  </read_first>
  <action>
    **DTOs:** Define request body for send: `{ toUserId: string }` (class or interface consistent with project DTO style). Define response DTOs for list endpoints (friend user id + fields matching how `users` or `search` returns minimal user — reuse existing user response shapes from codebase where possible).

    **friends.validation.ts:** Implement `IFriendsValidation` with methods reusing `IUsersValidation.userVerifiedValidation` and `userIdValidation` for params/body keys (`toUserId`, `fromUserId`, `userId` paths). Follow same export pattern as other `*.validation.ts` files.

    **FriendsController:** Create `IFriendsController` with handler methods bound as arrow properties (project convention). Inject `IFriendsService` in constructor. Each handler calls service and `sendResponse` / error responses using existing `BaseController` + `httpStatus` patterns. **HTTP mapping (lock these for executor):**
    - `POST /requests` body `{ toUserId }` → **201** on new pending request.
    - `POST /requests/:fromUserId/accept` → **200** on success.
    - `POST /requests/:fromUserId/decline` → **200** on delete.
    - `DELETE /requests/outgoing/:toUserId` → **200** revoke.
    - `DELETE /:userId` unfriend → **200**.
    - `GET /` list friends → **200** paginated.
    - `GET /requests/incoming` → **200**.
    - `GET /requests/outgoing` → **200**.

    **FriendsRoute** extends `BaseRoute`: register routes with `protect`, `appLimiter` on **all mutating** methods (POST, DELETE), and the same `userVerifiedValidation` ordering as `bookmarks.route.ts` (or another existing `BaseRoute` module). Paths **exactly** (relative to router mounted at `/friends` under API prefix):
    - `GET /`
    - `GET /requests/incoming`
    - `GET /requests/outgoing`
    - `POST /requests`
    - `POST /requests/:fromUserId/accept`
    - `POST /requests/:fromUserId/decline`
    - `DELETE /requests/outgoing/:toUserId`
    - `DELETE /:userId`

    Export default factory `() => new FriendsRoute().getRouter()` matching other route files.

    Controller methods may throw / delegate to `asyncHandler`; use existing error types for 400/403/404/409/429.

    **Executor:** It is acceptable for `npm run build` to fail after Task 1 if the controller references service methods not yet implemented; Task 2 must restore a passing build.
  </action>
  <verify>
    <automated>test -f /Users/dolong/Documents/NodeJS/nodejs-social/src/routes/friends.route.ts && test -f /Users/dolong/Documents/NodeJS/nodejs-social/src/controllers/friends.controller.ts && rg -q "extends BaseRoute" /Users/dolong/Documents/NodeJS/nodejs-social/src/routes/friends.route.ts</automated>
  </verify>
  <acceptance_criteria>
    - **Intentionally pre-compile:** Task 1 `<automated>` does not run `npm run build`. A full compile is **required** after Task 2 implements `IFriendsService` methods used by the controller.
    - `test -f src/routes/friends.route.ts && test -f src/controllers/friends.controller.ts && test -f src/validations/friends.validation.ts`
    - `rg "requests/incoming|requests/outgoing|requests/outgoing/" src/routes/friends.route.ts` returns at least 3 matches (one per substring or path segment as implemented).
    - `rg "class FriendsRoute|extends BaseRoute" src/routes/friends.route.ts` returns a match.
  </acceptance_criteria>
  <done>Route/controller/DTO/validation files exist with locked path structure.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: FriendsService mutations, UTC cap, policy; container + app mount</name>
  <files>src/services/friends.service.ts, src/container/index.ts, src/app.ts</files>
  <read_first>
    .planning/phases/02-friends-graph-privacy/02-CONTEXT.md
    src/services/friends.service.ts
    src/container/index.ts
    src/repositories/friendRequest.repository.ts
    src/repositories/friendship.repository.ts
    src/repositories/block.repository.ts
  </read_first>
  <action>
    **Extend `FriendsService` constructor** to accept `IFriendshipRepository`, `IFriendRequestRepository`, `IBlockRepository`, `RedisService` (or existing redis wrapper), and **`IUserRepository` or `IUsersService`** — whichever the codebase already uses to resolve user existence for targets (follow established pattern from other services). Update `IFriendsService` with methods called by controller:

    **UTC day helper (D-07):** Private `utcDayRange(now: Date): { start: Date; endExclusive: Date }` using `Date.UTC(y, m, d)` and `endExclusive = start + 86400000`. Document in **file-level comment** that cap is **calendar UTC day**, not local TZ.

    **sendFriendRequest(myUserId, toUserId):**
    - If `myUserId === toUserId` → **400** (D-11).
    - If `await blockRepo.isBlockedEitherWay(a,b)` → **403** (D-12; convention locked: **403** for blocked pair on graph mutations).
    - If friendship exists → **409** (D-10).
    - If `countOutgoingRequestsCreatedOnUtcDay` for `fromUserId` in current UTC window **>= 100** → **429** (FRND-05 / D-07).
    - `insertOne` pending `{ fromUserId, toUserId, createdAt: new Date() }`; duplicate key → **409** (D-06 race).

    **Product note (inverse requests):** If pending `(B→A)` exists while sending `(A→B)`, **allow both** (unique keys differ). Add **comment in code** documenting this choice per 02-RESEARCH open question.

    **acceptIncomingRequest(myUserId, fromUserId):** Current user is recipient. Verify pending doc `{ fromUserId, toUserId: myUserId }` exists; else **404**. Block check **403**. Then `insertOne` friendship normalized pair + `deleteDirectedRequest(fromUserId, myUserId)`. On friendship duplicate, treat idempotent **200** if request already gone (choose one consistent behavior; prefer transactional `bulkWrite` or ordered operations if available).

    **declineIncomingRequest(myUserId, fromUserId):** `deleteOne` pending `{ fromUserId, toUserId: myUserId }`; if `deletedCount === 0` → **404**.

    **revokeOutgoingRequest(myUserId, toUserId):** `deleteDirectedRequest(myUserId, toUserId)`; **404** if none.

    **unfriend(myUserId, otherUserId):** `deleteFriendshipPair`; **404** if no friendship.

    **listFriends / listIncoming / listOutgoing:** Query `friendRequests` and `friendships` with pagination (`page`/`limit` query params, defaults match posts/search). Populate user documents via user repo/service for returned ids (minimum: `_id`, `username` or equivalent fields present on user schema).

    **Cache:** After any mutation affecting graph for user U, call `invalidateFriendCache(U)` for **both** users when friendship or accept changes (accept: both; unfriend: both; send/decline/revoke: only sender/recipient caches if friend list unchanged — at minimum invalidate **myUserId** and **counterparty** on accept/unfriend; on send invalidate **myUserId** only).

    **Container:** Construct expanded `FriendsService` with new repos + block repo. Add `FriendsController` + `getFriendsController()` + `getFriendsValidation()` if separate. Wire route factory to container.

    **app.ts:** `import friendsRouter from '@/routes/friends.route'` and `router.use('/friends', friendsRouter());` inside `setupRoutes()`.

    Remove any stale `followersRouter` import if still present (should be gone after plan `02`).
  </action>
  <verify>
    <automated>cd /Users/dolong/Documents/NodeJS/nodejs-social && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - **Compile gate:** `npm run build` exits 0 after this task (covers Task 1 skeleton + Task 2 implementations).
    - `rg "router\\.use\\('/friends'" src/app.ts` returns a match.
    - `rg "isBlockedEitherWay" src/services/friends.service.ts` returns at least one match.
    - `rg "countOutgoingRequestsCreatedOnUtcDay|utcDayRange|UTC" src/services/friends.service.ts` returns at least one match for cap logic (count or comment with UTC).
    - `rg "429|TOO_MANY" src/controllers/friends.controller.ts src/services/friends.service.ts` OR service throws/middleware maps 429 — grep for `429` **or** verify via `httpStatus` constant usage for rate-like errors (executor must use **429** for cap exceeded).
  </acceptance_criteria>
  <done>All FRND behaviors implemented behind `/api/friends` with D-07 documented and D-12 enforced via block repository.</done>
</task>

</tasks>

## verification_criteria

- `npm run build` succeeds.
- `rg "/friends" src/app.ts src/routes/friends.route.ts` confirms mount and route file.
- Manual smoke (optional): `POST /api/friends/requests` with valid JWT returns 201 once, duplicate pending returns 409.

## success_criteria

- FRND-01–FRND-06 satisfied per ROADMAP success criteria items 1–2 and 3 (cap) and unfriend (item 4 partial; block HTTP in plan `04`).

<output>
After completion, create `.planning/phases/02-friends-graph-privacy/02-03-SUMMARY.md`
</output>
