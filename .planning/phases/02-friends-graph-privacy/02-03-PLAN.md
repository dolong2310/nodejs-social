---
phase: 02-friends-graph-privacy
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - src/services/blocks.service.ts
  - src/controllers/blocks.controller.ts
  - src/routes/blocks.route.ts
  - src/dtos/requests/blocks.request.dto.ts
  - src/dtos/responses/blocks.response.dto.ts
  - src/validations/blocks.validation.ts
  - src/container/index.ts
  - src/app.ts
  - swagger/paths.yaml
  - swagger/tags.yaml
  - swagger/components.yaml
  - swagger/operationId.yaml
  - swagger/swagger-docs.yaml
  - scripts/fake-data.ts
  - src/constants/message.constant.ts
autonomous: true
requirements:
  - INFR-02
  - BLCK-01
  - BLCK-03

must_haves:
  truths:
    - User can block another user; block removes any mutual friendship and all pending friend requests involving both users (D-14).
    - User can unblock (BLCK-03 / D-15); unblock does not recreate friendship.
    - OpenAPI/docs no longer expose `/followers`; friends and blocks paths are documented.
    - Fake-data script does not insert into `followers` collection or import follower schema.
    - Post feed and search queries remain unchanged from plan 02 (no BLCK-02 feed filtering in this phase beyond existing repository helpers from 02-01).
  artifacts:
    - path: src/routes/blocks.route.ts
      provides: POST/DELETE/GET under /api/blocks (via prefix)
    - path: swagger/paths.yaml
      provides: No `/followers` path definitions; `/friends` and `/blocks` defined
  key_links:
    - from: src/services/blocks.service.ts
      to: src/repositories/friendship.repository.ts
      via: deleteFriendshipPair on block (D-14)
    - from: src/services/blocks.service.ts
      to: src/repositories/friendRequest.repository.ts
      via: deleteAllRequestsInvolvingUsers on block (D-14)
---

## Objective

Deliver **`/api/blocks`** (D-03) for **BLCK-01** and **BLCK-03**, complete remaining **INFR-02** documentation and tooling (Swagger, `scripts/fake-data.ts`, grep-clean), and expose **block** services on the container for Phase 3 — **without** implementing post-feed `$nin` filtering (BLCK-02 read path stays Phase 3 per 02-CONTEXT D-16).

## Scope

- In: blocks HTTP stack, D-14 side effects, Swagger + yaml tags, fake-data seeding for friendships (not followers), user-visible message string updates where still “followers-only” in copy.
- Out: feed/search author exclusion by block (Phase 3), notification persistence (Phase 5).

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-friends-graph-privacy/02-CONTEXT.md
@.planning/phases/02-friends-graph-privacy/02-RESEARCH.md
@swagger/paths.yaml
@swagger/tags.yaml
@scripts/fake-data.ts
@src/services/friends.service.ts
@src/repositories/block.repository.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: BlocksService, controller, route, D-14 wiring, app mount</name>
  <files>src/services/blocks.service.ts, src/controllers/blocks.controller.ts, src/routes/blocks.route.ts, src/dtos/requests/blocks.request.dto.ts, src/dtos/responses/blocks.response.dto.ts, src/validations/blocks.validation.ts, src/container/index.ts, src/app.ts</files>
  <read_first>
    .planning/phases/02-friends-graph-privacy/02-CONTEXT.md
    src/services/friends.service.ts
    src/repositories/block.repository.ts
    src/repositories/friendship.repository.ts
    src/repositories/friendRequest.repository.ts
    src/routes/friends.route.ts
    src/container/index.ts
  </read_first>
  <action>
    **BlocksService** (`IBlocksService`): inject `IBlockRepository`, `IFriendshipRepository`, `IFriendRequestRepository`, `IFriendsService` (for `invalidateFriendCache` only) and redis if needed.

    **blockUser(blockerUserId: string, blockedUserId: string):**
    - Same user → **400**.
    - If `isBlockedEitherWay` already true → **409** (idempotent choice) or **200** — pick **409** for duplicate block insert attempt.
    - Order **D-14:** `deleteFriendshipPair(blocker, blocked)` then `deleteAllRequestsInvolvingUsers(blocker, blocked)` then `createBlock(blockerId, blockedId)`.
    - Call `invalidateFriendCache` for **both** string user ids after success.

    **unblockUser(blockerUserId, blockedUserId):** `deleteBlock`; if `deletedCount === 0` → **404**. Invalidate both users’ friend caches (safe no-op if empty).

    **listBlockedUsers(blockerUserId):** `listBlockedUserIdsForBlocker` + populate user summaries same pattern as friends list (reuse user repo/service).

    **HTTP (router mounted at `/blocks` under API prefix):**
    - `POST /` body `{ userId: string }` — **201** created.
    - `DELETE /:userId` — **200** unblocked.
    - `GET /` — **200** list.

    Use `protect`, `appLimiter` on POST/DELETE, `userVerifiedValidation`, `userIdValidation` for body `userId` and param `userId` (match `friends` patterns).

    **BlocksRoute** extends `BaseRoute`; default export factory like other routes.

    **Container:** construct `BlocksService`, `BlocksController`, register `getBlocksService(): IBlocksService` and `getBlocksController(): IBlocksController`, `getBlocksValidation()` if split.

    **app.ts:** `router.use('/blocks', blocksRouter());`

    **Convention:** Blocked users attempting friend operations are already **403** from plan 02; blocking user should also be **403** if reverse pending — already covered by `isBlockedEitherWay`.
  </action>
  <verify>
    <automated>cd /Users/dolong/Documents/NodeJS/nodejs-social && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "router\\.use\\('/blocks'" src/app.ts` returns a match.
    - `rg "deleteAllRequestsInvolvingUsers|deleteFriendshipPair" src/services/blocks.service.ts` returns at least one match for each substring.
    - `rg "createBlock|deleteBlock" src/services/blocks.service.ts` returns at least one match.
    - `rg "invalidateFriendCache" src/services/blocks.service.ts` returns at least one match.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>BLCK-01 and BLCK-03 behaviors available via HTTP; D-14 cleanup runs on block.</done>
</task>

<task type="auto">
  <name>Task 2: Swagger/OpenAPI — remove followers, add friends & blocks</name>
  <files>swagger/paths.yaml, swagger/tags.yaml, swagger/components.yaml, swagger/operationId.yaml, swagger/swagger-docs.yaml</files>
  <read_first>
    .planning/phases/02-friends-graph-privacy/02-CONTEXT.md
    swagger/paths.yaml
    swagger/tags.yaml
    src/routes/friends.route.ts
    src/routes/blocks.route.ts
  </read_first>
  <action>
    **Remove** all path entries under **`/followers`** and **`/followers/{userId}`** from `swagger/paths.yaml`. Remove **`followers`** tag from `swagger/tags.yaml` (replace with **`friends`** and **`blocks`** tags if not present).

    **Add** OpenAPI paths documenting at minimum:
    - All `/friends` routes from plan 02 (method, summary, tags: `[friends]`, security: bearer/JWT if project standard, requestBody for `POST /friends/requests` with `toUserId`, path params `fromUserId`, `toUserId`, `userId` as applicable).
    - All `/blocks` routes from Task 1 (`POST /blocks`, `DELETE /blocks/{userId}`, `GET /blocks`).

    **components/schemas:** Add or reuse user id string formats; document **429** response for friend-request cap with description referencing **100 requests per UTC calendar day** (D-07).

    Update `swagger/operationId.yaml` and `swagger/swagger-docs.yaml` so they contain **no** `followUser` / `unfollowUser` operation IDs tied to removed routes; add operationIds for new endpoints (names discretionary but unique).

    Ensure `getSwaggerDefinition` / jsdoc aggregation still loads (no YAML syntax errors).
  </action>
  <verify>
    <automated>cd /Users/dolong/Documents/NodeJS/nodejs-social && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "/followers" swagger/` returns **no** matches in path keys (literal `/followers` as YAML path segment). *Note:* audience enum string `followers` inside post schemas may remain until Phase 3 per CONTEXT deferral — grep scope is **`swagger/`** directory only; allow `followers` only inside **post audience enum** lines if still present, but **must not** have ` /followers` REST path.
    - `rg "^  /friends|/friends/" swagger/paths.yaml` returns at least one match.
    - `rg "^  /blocks|/blocks/" swagger/paths.yaml` returns at least one match.
    - `rg "name: friends|name: blocks" swagger/tags.yaml` returns at least one match for each tag name.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>INFR-02 satisfied for API documentation surface; followers REST docs removed.</done>
</task>

<task type="auto">
  <name>Task 3: fake-data, messages, final INFR-02 grep gates</name>
  <files>scripts/fake-data.ts, src/constants/message.constant.ts</files>
  <read_first>
    .planning/phases/02-friends-graph-privacy/02-CONTEXT.md
    scripts/fake-data.ts
    src/models/schemas/friendship.schema.ts
    src/constants/message.constant.ts
  </read_first>
  <action>
    **scripts/fake-data.ts:**
    - Remove imports/usages of `FollowerSchema` and **`db.followers.insertOne`** (collection must not be referenced).
    - Seed **mutual friendships** instead: insert into **`friendships`** using **normalized** `userIdLow`/`userIdHigh` from `FriendshipRepository.normalizeFriendshipPair` **or** duplicate the same sort rule inline in the script (must match app invariant).
    - Update inline comments that say “follow” for social graph to **friends** where they describe behavior.

    **message.constant.ts:** Rename or replace `ONLY_FOLLOWERS_CAN_VIEW_POSTS` (and `YOU_CANNOT_FOLLOW_YOURSELF` if still used) with **friends** wording **only if** references exist in `src/` — update all call sites to new constant names so build passes. If `YOU_CANNOT_FOLLOW_YOURSELF` is dead after follower removal, delete it.

    **Final repo grep (INFR-02):** From repo root, confirm:
    - `rg "followersRouter|/followers|FollowersController|FollowersService|FollowerRepository|follower\\.schema|get followers\\(" src/` → no matches.
    - `rg "followers:" src/` → no matches (Redis key pattern removed).
    - `rg "collection\\(['\\\"]followers['\\\"]\\)|\\.followers\\b" src scripts/` → no matches (no code references `followers` collection accessor or string literal collection name).

    If `EPostAudience.FOLLOWERS = 'followers'` remains in `src/enums/posts.enum.ts` per Phase 3 deferral, **do not** remove it in this task; it is not the Mongo collection.
  </action>
  <verify>
    <automated>cd /Users/dolong/Documents/NodeJS/nodejs-social && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "follower\\.schema|FollowerSchema|followers\\.insertOne|db\\.followers" scripts/fake-data.ts` returns no matches.
    - `rg "friendships" scripts/fake-data.ts` returns at least one match.
    - `rg "followersRouter|FollowersService|FollowerRepository|router\\.use\\('/followers'" src/` returns no matches.
    - `rg "followers:" src/constants/cache.constant.ts` returns no matches.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Scripts and constants align with friends graph; INFR-02 grep gates pass for `src/` + targeted `scripts/`.</done>
</task>

</tasks>

## verification_criteria

- `npm run build` succeeds.
- Swagger has friends + blocks paths; no followers REST paths in `swagger/paths.yaml`.
- `scripts/fake-data.ts` seeds friendships, not followers.
- Grep gates in Task 3 pass.

## success_criteria

- ROADMAP Phase 2 success criteria items **4** (block/unblock, auto-unfriend) and **6** (legacy follower removal) satisfied together with prior plans for items **1–3**.

<output>
After completion, create `.planning/phases/02-friends-graph-privacy/02-03-SUMMARY.md`
</output>
