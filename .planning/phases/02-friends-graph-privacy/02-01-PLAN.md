---
phase: 02-friends-graph-privacy
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/models/schemas/friendship.schema.ts
  - src/models/schemas/friendRequest.schema.ts
  - src/models/schemas/block.schema.ts
  - src/database/mongodb/database.service.ts
  - src/repositories/friendship.repository.ts
  - src/repositories/friendRequest.repository.ts
  - src/repositories/block.repository.ts
autonomous: true
requirements:
  - BLCK-02

must_haves:
  truths:
    - Mongo social DB exposes `friendships`, `friendRequests`, and `blocks` collections with indexes matching D-04, D-06, D-07, D-13 (see 02-CONTEXT.md).
    - Block repository exposes either-direction checks for Phase 3 (BLCK-02) without changing post feed queries in this plan.
  artifacts:
    - path: src/repositories/block.repository.ts
      provides: isBlockedEitherWay, listUserIdsBlockedInEitherDirection (names may match exactly or be equivalent exported methods)
  key_links:
    - from: src/database/mongodb/database.service.ts
      to: friendships/friendRequests/blocks collections
      via: getters + initializeIndexes
---

## Objective

Establish **friend graph persistence** (D-04–D-06, D-13 indexes) and **block query helpers** (BLCK-02 for Phase 3): schemas, `DatabaseService` accessors/indexes, and three repositories. **No** `FriendsService`, **no** `Container` / `app.ts` / posts / search rewires, **no** follower deletions (plan `02`).

## Scope

- In: schemas, `DatabaseService` collections/indexes, `FriendshipRepository`, `FriendRequestRepository`, `BlockRepository` (incl. BLCK-02 helpers per 02-RESEARCH.md).
- Out: cache keys, service layer, HTTP, follower removal, consumer rewiring (plan `02`); `/api/friends` and `/api/blocks` (plans `03`–`04`).

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-friends-graph-privacy/02-CONTEXT.md
@.planning/phases/02-friends-graph-privacy/02-RESEARCH.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/database/mongodb/database.service.ts
@src/models/schemas/follower.schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schemas and DatabaseService collections + indexes</name>
  <files>src/models/schemas/friendship.schema.ts, src/models/schemas/friendRequest.schema.ts, src/models/schemas/block.schema.ts, src/database/mongodb/database.service.ts</files>
  <read_first>
    .planning/phases/02-friends-graph-privacy/02-CONTEXT.md
    src/database/mongodb/database.service.ts
    src/models/schemas/follower.schema.ts
  </read_first>
  <action>
    Create **friendship.schema.ts**: document shape `{ userIdLow: ObjectId, userIdHigh: ObjectId, createdAt: Date }` with `userIdLow < userIdHigh` invariant documented in file comment (D-04).

    Create **friendRequest.schema.ts**: `{ fromUserId: ObjectId, toUserId: ObjectId, createdAt: Date }` (D-05).

    Create **block.schema.ts**: `{ blockerId: ObjectId, blockedId: ObjectId, createdAt: Date }` (D-13).

    In **database.service.ts**:
    - Do **not** remove the `followers` collection yet — removal happens in plan `02` together with index teardown and INFR-02 deletions (avoids breaking the build mid-phase). **Do** add typed getters `friendships`, `friendRequests`, `blocks` mapping to collection names **`friendships`**, **`friendRequests`**, **`blocks`**.
    - Add `createFriendshipIndexes`: unique `{ userIdLow: 1, userIdHigh: 1 }`; single-field indexes `{ userIdLow: 1 }` and `{ userIdHigh: 1 }`.
    - Add `createFriendRequestIndexes`: unique `{ fromUserId: 1, toUserId: 1 }`; `{ toUserId: 1, createdAt: -1 }`; `{ fromUserId: 1, createdAt: -1 }`; compound `{ fromUserId: 1, createdAt: 1 }` for UTC-day cap counts (D-07).
    - Add `createBlockIndexes`: unique `{ blockerId: 1, blockedId: 1 }`; `{ blockerId: 1 }`; `{ blockedId: 1 }`.
    - Register the three `create*Indexes` calls in `initializeIndexes()` alongside existing index creators.

    Use existing `indexExists` / `createIndex` patterns already used for other collections in this file.
  </action>
  <verify>
    <automated>cd /Users/dolong/Documents/NodeJS/nodejs-social && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "friendships|friendRequests|blocks" src/database/mongodb/database.service.ts` returns at least one match for each of the three collection names as string literals.
    - `rg "createFriendshipIndexes|createFriendRequestIndexes|createBlockIndexes" src/database/mongodb/database.service.ts` returns a match for each function name.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Social DB accessors and indexes for graph + blocks exist alongside legacy followers until plan `02` removes them.</done>
</task>

<task type="auto">
  <name>Task 2: Repositories — friendship, friendRequest, block (BLCK-02 hooks)</name>
  <files>src/repositories/friendship.repository.ts, src/repositories/friendRequest.repository.ts, src/repositories/block.repository.ts</files>
  <read_first>
    .planning/phases/02-friends-graph-privacy/02-CONTEXT.md
    src/repositories/follower.repository.ts
    src/repositories/user.repository.ts
  </read_first>
  <action>
    Implement **FriendshipRepository** extending `BaseRepository` (same pattern as follower repo):
    - Export `normalizeFriendshipPair(a: ObjectId, b: ObjectId): { userIdLow: ObjectId; userIdHigh: ObjectId }` using **BSON ObjectId byte order** (e.g. `Buffer.compare(a.id, b.id)`), not string `localeCompare` — document in comment.
    - `findFriendUserIdsForUser(userId: ObjectId): Promise<ObjectId[]>` — union of neighbors: all `userIdHigh` where `userIdLow = userId` plus all `userIdLow` where `userIdHigh = userId` (two queries or one `$or`, either is fine).
    - `findFriendshipPair(userIdA: ObjectId, userIdB: ObjectId): Promise<Document | null>` — lookup by normalized low/high.
    - `deleteFriendshipPair(userIdA: ObjectId, userIdB: ObjectId): Promise<void>` — `deleteOne` normalized pair.

    Implement **FriendRequestRepository**:
    - `countOutgoingRequestsCreatedOnUtcDay(fromUserId: ObjectId, dayStart: Date, dayEndExclusive: Date): Promise<number>` — `countDocuments({ fromUserId, createdAt: { $gte: dayStart, $lt: dayEndExclusive } })`.
    - `deleteDirectedRequest(fromUserId: ObjectId, toUserId: ObjectId): Promise<void>`.
    - `deleteAllRequestsInvolvingUsers(userA: ObjectId, userB: ObjectId): Promise<void>` — delete any request where `(fromUserId, toUserId)` involves both users (use `$or` with four clauses or `$and` with `$in` on both fields covering both users).
    - Stub or minimal additional find helpers if needed for plan `03` (may be empty placeholders with TODO only if unused — prefer adding `findPendingByDirectedPair` returning one doc).

    Implement **BlockRepository** (BLCK-02 per 02-RESEARCH.md):
    - `isBlockedEitherWay(a: ObjectId, b: ObjectId): Promise<boolean>` — `findOne` with `$or: [{ blockerId: a, blockedId: b }, { blockerId: b, blockedId: a }]` (limit 1).
    - `listUserIdsBlockedInEitherDirection(viewerId: ObjectId): Promise<ObjectId[]>` — distinct `blockedId` where `blockerId = viewerId`, union distinct `blockerId` where `blockedId = viewerId`, dedupe to unique list.
    - `createBlock(blockerId: ObjectId, blockedId: ObjectId): Promise<void>` and `deleteBlock(blockerId: ObjectId, blockedId: ObjectId): Promise<void>` for use in plan `04` (blocks HTTP) — implement now.
    - `listBlockedUserIdsForBlocker(blockerId: ObjectId): Promise<ObjectId[]>` for future list endpoint.

    All repos use `this.db` from `BaseRepository` / `DatabaseService` getters added in Task 1.
  </action>
  <verify>
    <automated>cd /Users/dolong/Documents/NodeJS/nodejs-social && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Files exist: `src/repositories/friendship.repository.ts`, `src/repositories/friendRequest.repository.ts`, `src/repositories/block.repository.ts`.
    - `rg "isBlockedEitherWay|listUserIdsBlockedInEitherDirection" src/repositories/block.repository.ts` returns at least one match for each method name.
    - `rg "normalizeFriendshipPair" src/repositories/friendship.repository.ts` returns a match.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Repositories implement normalized friendship reads/deletes, request counting/deletion helpers, and symmetric block queries for Phase 3.</done>
</task>

</tasks>

## verification_criteria

- `npm run build` succeeds after all tasks.
- `block.repository` exports either-direction helpers required by BLCK-02 for Phase 3.

## success_criteria

- BLCK-02 **repository-layer** symmetric block queries implemented and callable from future services.
- Indexes and collections for `friendships`, `friendRequests`, and `blocks` are registered on `DatabaseService`.

<output>
After completion, create `.planning/phases/02-friends-graph-privacy/02-01-SUMMARY.md`
</output>
