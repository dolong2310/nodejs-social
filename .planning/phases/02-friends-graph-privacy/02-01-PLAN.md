---
phase: 02-friends-graph-privacy
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/models/schemas/friendship.schema.ts
  - src/models/schemas/friendRequest.schema.ts
  - src/models/schemas/block.schema.ts
  - src/database/mongodb/database.service.ts
  - src/repositories/friendship.repository.ts
  - src/repositories/friendRequest.repository.ts
  - src/repositories/block.repository.ts
  - src/services/friends.service.ts
  - src/constants/cache.constant.ts
  - src/container/index.ts
  - src/app.ts
  - src/controllers/posts.controller.ts
  - src/services/search.service.ts
  - src/validations/posts.validation.ts
  - src/routes/followers.route.ts
  - src/controllers/followers.controller.ts
  - src/services/followers.service.ts
  - src/repositories/follower.repository.ts
  - src/models/schemas/follower.schema.ts
  - src/dtos/requests/follower.request.dto.ts
  - src/dtos/responses/follower.response.dto.ts
autonomous: true
requirements:
  - INFR-02
  - BLCK-02

must_haves:
  truths:
    - Mongo social DB exposes `friendships`, `friendRequests`, and `blocks` collections with indexes matching D-04, D-06, D-07, D-13 (see 02-CONTEXT.md).
    - No application code references `followers` collection, `FollowerRepository`, `FollowersService`, or `/api/followers` mount.
    - Post feed and post visibility checks use mutual friendship (normalized pair) instead of asymmetric follow edges.
    - Block repository exposes either-direction checks for Phase 3 (BLCK-02) without changing post feed queries in this plan.
  artifacts:
    - path: src/repositories/block.repository.ts
      provides: isBlockedEitherWay, listUserIdsBlockedInEitherDirection (names may match exactly or be equivalent exported methods)
    - path: src/services/friends.service.ts
      provides: friend id list + friendship check for posts/search/validation
  key_links:
    - from: src/container/index.ts
      to: src/services/friends.service.ts
      via: constructor injection replacing followers stack
    - from: src/database/mongodb/database.service.ts
      to: friendships/friendRequests/blocks collections
      via: getters + initializeIndexes
---

## Objective

Establish **friend graph persistence** (D-04–D-06, D-13 indexes), **block query helpers** (BLCK-02 for Phase 3), and complete **INFR-02** removal of the legacy **followers** module, Redis key pattern `followers:${userId}`, and `/api/followers` registration — while keeping **posts** and **search** working via `FriendsService` read APIs (per D-01, follower pattern replacement). **No** `/api/friends` or `/api/blocks` HTTP surface in this plan.

## Scope

- In: schemas, `DatabaseService` collections/indexes, three repositories, minimal `FriendsService` (read + cache only), container and consumer rewiring, delete follower-only files from 02-CONTEXT canonical_refs.
- Out: friend request mutations, block HTTP, Swagger updates, `scripts/fake-data.ts` (plan 03).

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-friends-graph-privacy/02-CONTEXT.md
@.planning/phases/02-friends-graph-privacy/02-RESEARCH.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/database/mongodb/database.service.ts
@src/container/index.ts
@src/routes/followers.route.ts
@src/services/followers.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schemas and DatabaseService collections + indexes</name>
  <files>src/models/schemas/friendship.schema.ts, src/models/schemas/friendRequest.schema.ts, src/models/schemas/block.schema.ts, src/database/mongodb/database.service.ts</files>
  <read_first>
    .planning/phases/02-friends-graph-privacy/02-CONTEXT.md
    src/database/mongodb/database.service.ts
    src/models/schemas/follower.schema.ts
  </read_first>
  <action>
    Create **friendship.schema.ts**: document shape `{ userIdLow: ObjectId, userIdHigh: ObjectId, createdAt: Date }` with `userIdLow < userIdHigh` invariant documented in file comment (D-04).

    Create **friendRequest.schema.ts**: `{ fromUserId: ObjectId, toUserId: ObjectId, createdAt: Date }` (D-05).

    Create **block.schema.ts**: `{ blockerId: ObjectId, blockedId: ObjectId, createdAt: Date }` (D-13).

    In **database.service.ts**:
    - Remove import of `IFollower` and the `followers` collection getter; remove `createFollowersIndex` from the class interface and implementation; remove `createFollowersIndex()` from the `initializeIndexes()` `Promise.all` (or equivalent) list.
    - Add typed getters `friendships`, `friendRequests`, `blocks` mapping to collection names **`friendships`**, **`friendRequests`**, **`blocks`**.
    - Add `createFriendshipIndexes`: unique `{ userIdLow: 1, userIdHigh: 1 }`; single-field indexes `{ userIdLow: 1 }` and `{ userIdHigh: 1 }`.
    - Add `createFriendRequestIndexes`: unique `{ fromUserId: 1, toUserId: 1 }`; `{ toUserId: 1, createdAt: -1 }`; `{ fromUserId: 1, createdAt: -1 }`; compound `{ fromUserId: 1, createdAt: 1 }` for UTC-day cap counts (D-07).
    - Add `createBlockIndexes`: unique `{ blockerId: 1, blockedId: 1 }`; `{ blockerId: 1 }`; `{ blockedId: 1 }`.
    - Register the three `create*Indexes` calls in `initializeIndexes()` alongside existing index creators.

    Use existing `indexExists` / `createIndex` patterns already used for other collections in this file.
  </action>
  <verify>
    <automated>cd /Users/dolong/Documents/NodeJS/nodejs-social && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "followers" src/database/mongodb/database.service.ts` returns **no** matches (collection name or getter name removed from this file).
    - `rg "friendships|friendRequests|blocks" src/database/mongodb/database.service.ts` returns at least one match for each of the three collection names as string literals.
    - `rg "createFriendshipIndexes|createFriendRequestIndexes|createBlockIndexes" src/database/mongodb/database.service.ts` returns a match for each function name.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Social DB accessors and indexes for graph + blocks exist; followers index creation removed from code.</done>
</task>

<task type="auto">
  <name>Task 2: Repositories — friendship, friendRequest, block (BLCK-02 hooks)</name>
  <files>src/repositories/friendship.repository.ts, src/repositories/friendRequest.repository.ts, src/repositories/block.repository.ts</files>
  <read_first>
    .planning/phases/02-friends-graph-privacy/02-CONTEXT.md
    src/repositories/follower.repository.ts
    src/repositories/user.repository.ts
  </read_first>
  <action>
    Implement **FriendshipRepository** extending `BaseRepository` (same pattern as follower repo):
    - Export `normalizeFriendshipPair(a: ObjectId, b: ObjectId): { userIdLow: ObjectId; userIdHigh: ObjectId }` using **BSON ObjectId byte order** (e.g. `Buffer.compare(a.id, b.id)`), not string `localeCompare` — document in comment.
    - `findFriendUserIdsForUser(userId: ObjectId): Promise<ObjectId[]>` — union of neighbors: all `userIdHigh` where `userIdLow = userId` plus all `userIdLow` where `userIdHigh = userId` (two queries or one `$or`, either is fine).
    - `findFriendshipPair(userIdA: ObjectId, userIdB: ObjectId): Promise<Document | null>` — lookup by normalized low/high.
    - `deleteFriendshipPair(userIdA: ObjectId, userIdB: ObjectId): Promise<void>` — `deleteOne` normalized pair.

    Implement **FriendRequestRepository**:
    - `countOutgoingRequestsCreatedOnUtcDay(fromUserId: ObjectId, dayStart: Date, dayEndExclusive: Date): Promise<number>` — `countDocuments({ fromUserId, createdAt: { $gte: dayStart, $lt: dayEndExclusive } })`.
    - `deleteDirectedRequest(fromUserId: ObjectId, toUserId: ObjectId): Promise<void>`.
    - `deleteAllRequestsInvolvingUsers(userA: ObjectId, userB: ObjectId): Promise<void>` — delete any request where `(fromUserId, toUserId)` involves both users (use `$or` with four clauses or `$and` with `$in` on both fields covering both users).
    - Stub or minimal additional find helpers if needed for plan 02 (may be empty placeholders with TODO only if unused — prefer adding `findPendingByDirectedPair` returning one doc).

    Implement **BlockRepository** (BLCK-02 per 02-RESEARCH.md):
    - `isBlockedEitherWay(a: ObjectId, b: ObjectId): Promise<boolean>` — `findOne` with `$or: [{ blockerId: a, blockedId: b }, { blockerId: b, blockedId: a }]` (limit 1).
    - `listUserIdsBlockedInEitherDirection(viewerId: ObjectId): Promise<ObjectId[]>` — distinct `blockedId` where `blockerId = viewerId`, union distinct `blockerId` where `blockedId = viewerId`, dedupe to unique list.
    - `createBlock(blockerId: ObjectId, blockedId: ObjectId): Promise<void>` and `deleteBlock(blockerId: ObjectId, blockedId: ObjectId): Promise<void>` for use in plan 03 (implement now).
    - `listBlockedUserIdsForBlocker(blockerId: ObjectId): Promise<ObjectId[]>` for future list endpoint.

    All repos use `this.db` from `BaseRepository` / `DatabaseService` getters added in Task 1.
  </action>
  <verify>
    <automated>cd /Users/dolong/Documents/NodeJS/nodejs-social && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Files exist: `src/repositories/friendship.repository.ts`, `src/repositories/friendRequest.repository.ts`, `src/repositories/block.repository.ts`.
    - `rg "isBlockedEitherWay|listUserIdsBlockedInEitherDirection" src/repositories/block.repository.ts` returns at least one match for each method name.
    - `rg "normalizeFriendshipPair" src/repositories/friendship.repository.ts` returns a match.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Repositories implement normalized friendship reads/deletes, request counting/deletion helpers, and symmetric block queries for Phase 3.</done>
</task>

<task type="auto">
  <name>Task 3: FriendsService read path, remove followers stack, rewire app</name>
  <files>src/services/friends.service.ts, src/constants/cache.constant.ts, src/container/index.ts, src/app.ts, src/controllers/posts.controller.ts, src/services/search.service.ts, src/validations/posts.validation.ts, and deletions listed in frontmatter</files>
  <read_first>
    .planning/phases/02-friends-graph-privacy/02-CONTEXT.md
    src/services/followers.service.ts
    src/container/index.ts
    src/app.ts
    src/constants/cache.constant.ts
    src/controllers/posts.controller.ts
    src/validations/posts.validation.ts
  </read_first>
  <action>
    **Cache (per D-01 / INFR-02):** In `src/constants/cache.constant.ts`, remove the `followers:` key factory `followers:${userId}`. Add `friends: (userId: string) => \`friends:${userId}\`` (exact Redis key pattern). Rename TTL key `FOLLOWERS` to **`FRIENDS_GRAPH`** (or keep numeric value, update all references) so no exported identifier contains `FOLLOWERS` for cache TTL.

    **FriendsService** (`src/services/friends.service.ts`): new class implementing:
    - `findFollowedUserIds(userId: string): Promise<ObjectId[]>` — same **semantic contract** as old method: IDs of users mutually befriended with `userId`. Use Redis: get string[] from `CACHE_KEYS.friends(userId)`; on miss, load via `FriendshipRepository.findFriendUserIdsForUser`, map to string[] for cache, set with TTL from renamed constant, return `ObjectId[]`.
    - `isFriendOf(viewerUserId: string, otherUserId: string): Promise<boolean>` — true iff friendship pair exists (use repository `findFriendshipPair`). Used to replace `findFollowerId` checks in posts validation.
    - Export `IFriendsService` with these two methods plus `invalidateFriendCache(userId: string): Promise<void>` delegating to `redis.del(CACHE_KEYS.friends(userId))` for plan 02/03 mutations.

    **Delete files** (canonical INFR-02 list):  
    `src/routes/followers.route.ts`, `src/controllers/followers.controller.ts`, `src/services/followers.service.ts`, `src/repositories/follower.repository.ts`, `src/models/schemas/follower.schema.ts`, `src/dtos/requests/follower.request.dto.ts`, `src/dtos/responses/follower.response.dto.ts`.

    **src/app.ts:** Remove `import followersRouter` and `router.use('/followers', followersRouter());` entirely. Do **not** mount `/friends` yet (plan 02).

    **Container:** Remove `FollowerRepository`, `FollowersService`, `FollowersController` imports, fields, getters, and construction. Add `FriendshipRepository`, `FriendRequestRepository`, `BlockRepository`, `FriendsService` construction: `FriendsService` receives `friendshipRepository`, `redis` (same constructor shape as followers). Wire `SearchService(..., friendsService, ...)`, `PostsController(..., friendsService)`, `PostsValidation(..., friendsService)` replacing `IFollowersService` types with `IFriendsService`.

    **posts.controller.ts:** Replace `IFollowersService` with `IFriendsService`; keep variable name `friendsService` or equivalent; call `findFollowedUserIds` (still valid method name for minimal post.repository churn).

    **posts.validation.ts:** Replace follower check with `isFriendOf` for audience `EPostAudience.FOLLOWERS` gate (semantic: **friends-only** audience until enum rename in Phase 3 — per deferred). Update comment that still says "followers" to say **friends** where it describes behavior.

    **search.service.ts:** Replace `IFollowersService` with `IFriendsService`; keep passing `findFollowedUserIds` into repository callbacks (property name `findFollowedUserIds` may remain on the service for minimal diff).

    **messages:** Optionally update `ONLY_FOLLOWERS_CAN_VIEW_POSTS` string in `message.constant.ts` to friends-oriented wording only if referenced after validation changes; if grep shows unused, leave for plan 03.

    Ensure `npm run build` passes with zero imports of `@/services/followers.service` or `@/repositories/follower.repository`.
  </action>
  <verify>
    <automated>cd /Users/dolong/Documents/NodeJS/nodejs-social && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f src/routes/followers.route.ts && test ! -f src/services/followers.service.ts` (both deleted).
    - `rg "followers\\.route|/followers|FollowersService|FollowerRepository|from '@/services/followers" src/` returns **no** matches.
    - `rg "followers:" src/constants/cache.constant.ts` returns no matches.
    - `rg "friends:" src/constants/cache.constant.ts` returns at least one match (new Redis key factory).
    - `rg "router\\.use\\('/followers'" src/app.ts` returns no matches.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Followers stack removed; app compiles; posts and search use cached mutual friend IDs from `friendships` collection.</done>
</task>

</tasks>

## verification_criteria

- `npm run build` succeeds after all tasks.
- No `src/` references to `/api/followers`, `followers` collection getter, or `followers:` Redis key factory.
- `block.repository` exports either-direction helpers required by BLCK-02 for Phase 3.

## success_criteria

- INFR-02 **code and persistence surface** for followers removed from runtime paths listed in 02-CONTEXT canonical_refs (Swagger and fake-data completed in plan 03).
- BLCK-02 **repository-layer** symmetric block queries implemented and callable from future services.

<output>
After completion, create `.planning/phases/02-friends-graph-privacy/02-01-SUMMARY.md`
</output>
