---
phase: 02-friends-graph-privacy
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/services/friends.service.ts
  - src/constants/cache.constant.ts
  - src/container/index.ts
  - src/app.ts
  - src/controllers/posts.controller.ts
  - src/services/search.service.ts
  - src/validations/posts.validation.ts
  - src/routes/followers.route.ts
  - src/controllers/followers.controller.ts
  - src/services/followers.service.ts
  - src/repositories/follower.repository.ts
  - src/models/schemas/follower.schema.ts
  - src/dtos/requests/follower.request.dto.ts
  - src/dtos/responses/follower.response.dto.ts
autonomous: true
requirements:
  - INFR-02

must_haves:
  truths:
    - No application code references `followers` collection, `FollowerRepository`, `FollowersService`, or `/api/followers` mount after this plan.
    - Post feed and post visibility checks use mutual friendship (normalized pair) instead of asymmetric follow edges.
    - `FriendsService` read path + cache back the same consumer contracts as the removed followers stack (posts/search/validation).
  artifacts:
    - path: src/services/friends.service.ts
      provides: findFollowedUserIds (mutual friends), isFriendOf, invalidateFriendCache for downstream plans
  key_links:
    - from: src/container/index.ts
      to: src/services/friends.service.ts
      via: constructor injection replacing followers stack
    - from: src/database/mongodb/database.service.ts
      to: friendships/friendRequests/blocks collections
      via: getters + initializeIndexes (followers removed in this plan’s database.service edits)
---

## Objective

Complete **INFR-02** runtime removal of the legacy **followers** module, Redis key pattern `followers:${userId}`, and `/api/followers` registration; add minimal **`FriendsService`** read APIs + cache so **posts** and **search** keep working (per D-01). Includes **`database.service.ts`** teardown of `followers` collection getter and `createFollowersIndex`. **No** `/api/friends` or `/api/blocks` HTTP surface (plans `03`–`04`).

## Scope

- In: `FriendsService` (read + cache + `invalidateFriendCache`), cache constants, `DatabaseService` followers removal, `Container` + `app.ts` + posts/search rewiring, delete follower-only files from 02-CONTEXT canonical_refs.
- Out: friend request mutations, block HTTP, Swagger, `scripts/fake-data.ts` (plan `04`).

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-friends-graph-privacy/02-CONTEXT.md
@.planning/phases/02-friends-graph-privacy/02-RESEARCH.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/database/mongodb/database.service.ts
@src/container/index.ts
@src/routes/followers.route.ts
@src/services/followers.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: FriendsService read path + cache constants</name>
  <files>src/services/friends.service.ts, src/constants/cache.constant.ts</files>
  <read_first>
    .planning/phases/02-friends-graph-privacy/02-CONTEXT.md
    src/services/followers.service.ts
    src/constants/cache.constant.ts
    src/repositories/friendship.repository.ts
  </read_first>
  <action>
    **Cache (per D-01 / INFR-02):** In `src/constants/cache.constant.ts`, remove the `followers:` key factory `followers:${userId}`. Add `friends: (userId: string) => \`friends:${userId}\`` (exact Redis key pattern). Rename TTL key `FOLLOWERS` to **`FRIENDS_GRAPH`** (or keep numeric value, update all references) so no exported identifier contains `FOLLOWERS` for cache TTL.

    **FriendsService** (`src/services/friends.service.ts`): new class implementing:
    - Constructor receives `IFriendshipRepository` and Redis (same shape as former `FollowersService` where practical).
    - `findFollowedUserIds(userId: string): Promise<ObjectId[]>` — IDs of users **mutually** befriended with `userId`. Use Redis: get string[] from `CACHE_KEYS.friends(userId)`; on miss, load via `FriendshipRepository.findFriendUserIdsForUser`, map to string[] for cache, set with TTL from renamed constant, return `ObjectId[]`.
    - `isFriendOf(viewerUserId: string, otherUserId: string): Promise<boolean>` — true iff friendship pair exists (use `findFriendshipPair`). Used to replace `findFollowerId` checks in posts validation (wired in Task 2).
    - Export `IFriendsService` with these methods plus `invalidateFriendCache(userId: string): Promise<void>` delegating to `redis.del(CACHE_KEYS.friends(userId))` for plan `03`/`04` mutations.

    Keep the interface minimal enough that plan `03` can extend the same file with mutation methods without renaming the type.
  </action>
  <verify>
    <automated>cd /Users/dolong/Documents/NodeJS/nodejs-social && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "followers:" src/constants/cache.constant.ts` returns no matches.
    - `rg "friends:" src/constants/cache.constant.ts` returns at least one match (new Redis key factory).
    - `rg "invalidateFriendCache|findFollowedUserIds|isFriendOf" src/services/friends.service.ts` returns at least one match for each substring.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Read path and cache keys for mutual friends exist; TTL naming has no `FOLLOWERS` identifier.</done>
</task>

<task type="auto">
  <name>Task 2: Remove followers from DatabaseService, delete follower module, app mount, Container wiring</name>
  <files>src/database/mongodb/database.service.ts, src/container/index.ts, src/app.ts, src/routes/followers.route.ts, src/controllers/followers.controller.ts, src/services/followers.service.ts, src/repositories/follower.repository.ts, src/models/schemas/follower.schema.ts, src/dtos/requests/follower.request.dto.ts, src/dtos/responses/follower.response.dto.ts</files>
  <read_first>
    .planning/phases/02-friends-graph-privacy/02-CONTEXT.md
    src/container/index.ts
    src/app.ts
    src/database/mongodb/database.service.ts
    src/services/friends.service.ts
  </read_first>
  <action>
    **database.service.ts:** Remove import of `IFollower` and the `followers` collection getter; remove `createFollowersIndex` from the class interface and implementation; remove `createFollowersIndex()` from the `initializeIndexes()` `Promise.all` (or equivalent) list. Followers collection must not be exposed after this task.

    **Delete files** (canonical INFR-02 list):  
    `src/routes/followers.route.ts`, `src/controllers/followers.controller.ts`, `src/services/followers.service.ts`, `src/repositories/follower.repository.ts`, `src/models/schemas/follower.schema.ts`, `src/dtos/requests/follower.request.dto.ts`, `src/dtos/responses/follower.response.dto.ts`.

    **src/app.ts:** Remove `import followersRouter` and `router.use('/followers', followersRouter());` entirely. Do **not** mount `/friends` yet (plan `03`).

    **Container:** Remove `FollowerRepository`, `FollowersService`, `FollowersController` imports, fields, getters, and construction. Add `FriendshipRepository`, `FriendRequestRepository`, `BlockRepository`, `FriendsService` construction: `FriendsService` receives `friendshipRepository`, `redis` (same constructor shape as followers). Wire `SearchService`, `PostsController`, and `PostsValidation` with **`IFriendsService`** and pass `this.friendsService` (replace every `IFollowersService` / followers service reference). Task 3 only adjusts **implementation** inside posts/search/validation files (`isFriendOf`, comments) if anything still references follower semantics after this wiring.
  </action>
  <verify>
    <automated>cd /Users/dolong/Documents/NodeJS/nodejs-social && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f src/routes/followers.route.ts && test ! -f src/services/followers.service.ts` (both deleted).
    - `rg "FollowersService|FollowerRepository|from '@/services/followers" src/container/index.ts` returns **no** matches.
    - `rg "followers" src/database/mongodb/database.service.ts` returns **no** matches (collection name or getter removed from this file).
    - `rg "router\\.use\\('/followers'" src/app.ts` returns no matches.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Follower files deleted; DB and app no longer expose followers; Container wires FriendsService and graph repos.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire posts and search to IFriendsService semantics</name>
  <files>src/controllers/posts.controller.ts, src/services/search.service.ts, src/validations/posts.validation.ts</files>
  <read_first>
    .planning/phases/02-friends-graph-privacy/02-CONTEXT.md
    src/controllers/posts.controller.ts
    src/validations/posts.validation.ts
    src/services/search.service.ts
    src/services/friends.service.ts
    src/container/index.ts
  </read_first>
  <action>
    **posts.controller.ts:** Ensure `IFriendsService`; call `findFollowedUserIds` (keep method name for minimal post.repository churn).

    **posts.validation.ts:** Replace follower check with `isFriendOf` for audience `EPostAudience.FOLLOWERS` gate (semantic: **friends-only** audience until enum rename in Phase 3 — per deferred). Update comments that still say "followers" to say **friends** where they describe behavior.

    **search.service.ts:** Ensure `IFriendsService`; keep passing `findFollowedUserIds` into repository callbacks.

    **messages:** Optionally update `ONLY_FOLLOWERS_CAN_VIEW_POSTS` in `message.constant.ts` only if referenced after validation changes; if grep shows unused, defer to plan `04`.

    Ensure `npm run build` passes with zero imports of `@/services/followers.service` or `@/repositories/follower.repository` anywhere under `src/`.
  </action>
  <verify>
    <automated>cd /Users/dolong/Documents/NodeJS/nodejs-social && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "IFollowersService|FollowersService" src/controllers/posts.controller.ts src/services/search.service.ts src/validations/posts.validation.ts` returns **no** matches.
    - `rg "followers\\.route|/api/followers" src/` returns **no** matches.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Posts and search use cached mutual friend IDs from `friendships` collection via FriendsService.</done>
</task>

</tasks>

## verification_criteria

- `npm run build` succeeds after Tasks 1–3.
- No `src/` references to `/api/followers`, `followers` collection getter, or `followers:` Redis key factory.

## success_criteria

- INFR-02 **code and persistence surface** for followers removed from runtime paths listed in 02-CONTEXT canonical_refs (Swagger and fake-data completed in plan `04`).

<output>
After completion, create `.planning/phases/02-friends-graph-privacy/02-02-SUMMARY.md`
</output>
