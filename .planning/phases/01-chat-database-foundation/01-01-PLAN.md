---
phase: 01-chat-database-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/envConfig.ts
  - .env.example
  - src/types/app.type.ts
  - src/index.ts
  - src/database/mongodb/index.ts
  - src/database/mongodb/database.service.ts
  - scripts/fake-data.ts
autonomous: true
requirements:
  - INFR-01
  - INFR-03
user_setup:
  - MongoDB cluster reachable with DATABASE_URI; create or choose a second database name for DATABASE_CHAT_NAME (may be empty of collections).

must_haves:
  truths:
    - Application startup fails if DATABASE_CHAT_NAME is missing from process environment (same fail-fast pattern as other ENV_KEYS).
    - MongoClient connects once; social and chat data use two Db handles from that client; both receive ping during DatabaseService.connect().
    - Chat database is not passed through initializeIndexes() (no chat collection indexes in Phase 1).
  artifacts:
    - src/config/envConfig.ts contains the literal string DATABASE_CHAT_NAME in ENV_KEYS array.
    - .env.example contains the literal line DATABASE_CHAT_NAME="" (with optional comment on same or preceding line documenting same-cluster usage).
    - src/database/mongodb/database.service.ts exposes read access to the chat Db (e.g. getter chatDb or getChatDb() returning mongodb.Db).
  key_links:
    - src/index.ts passes envConfig.DATABASE_CHAT_NAME into DatabaseInstance.init alongside uri and social databaseName.
    - DatabaseInstance.init forwards chat database name into DatabaseService constructor.
---

<objective>
Wire **INFR-01** and **INFR-03**: mandatory `DATABASE_CHAT_NAME`, strict env validation, second `Db` on the existing `MongoClient`, dual ping on connect (fail-fast if chat unreachable), and documented example env — without creating chat collections or indexes.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-chat-database-foundation/01-CONTEXT.md
@.planning/phases/01-chat-database-foundation/01-RESEARCH.md
@src/config/envConfig.ts
@src/types/app.type.ts
@src/index.ts
@src/database/mongodb/index.ts
@src/database/mongodb/database.service.ts
@src/app.ts
@scripts/fake-data.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DATABASE_CHAT_NAME to strict env and .env.example</name>
  <files>src/config/envConfig.ts, .env.example</files>
  <read_first>src/config/envConfig.ts, .env.example</read_first>
  <action>
    In `ENV_KEYS` (src/config/envConfig.ts), insert the string `DATABASE_CHAT_NAME` immediately after `DATABASE_NAME` inside the existing Database section comment block.
    In `.env.example`, after the `DATABASE_NAME=""` line, add a new line `DATABASE_CHAT_NAME=""` and a comment line above it stating that this is the MongoDB database name for chat data on the same cluster as `DATABASE_URI` (wording may vary but must mention same cluster / URI and chat DB purpose).
  </action>
  <verify>rg "DATABASE_CHAT_NAME" src/config/envConfig.ts .env.example</verify>
  <acceptance_criteria>
    - File `src/config/envConfig.ts` contains `DATABASE_CHAT_NAME` as an element of the `ENV_KEYS` array (grep: `'DATABASE_CHAT_NAME'` inside the array).
    - File `.env.example` contains a line matching `DATABASE_CHAT_NAME=""` or `DATABASE_CHAT_NAME=`.
    - File `.env.example` contains the substring `DATABASE_URI` in a comment near `DATABASE_CHAT_NAME` OR on the preceding comment line for the Database section.
  </acceptance_criteria>
  <done>envConfig throws on missing DATABASE_CHAT_NAME; example env documents the variable.</done>
</task>

<task type="auto">
  <name>Task 2: Second Db handle, dual ping, chat accessor on DatabaseService</name>
  <files>src/database/mongodb/database.service.ts</files>
  <read_first>src/database/mongodb/database.service.ts, .planning/phases/01-chat-database-foundation/01-CONTEXT.md</read_first>
  <action>
    Change `DatabaseService` constructor parameter type from `{ uri: string; databaseName: string }` to `{ uri: string; databaseName: string; chatDatabaseName: string }`.
    Keep a single `MongoClient`; set `this.db = this.client.db(databaseName)` for social; add `private readonly chatDb: Db` assigned as `this.client.db(chatDatabaseName)`.
    In `connect()`: after a successful `ping` on social `this.db`, run `await this.chatDb.command({ ping: 1 })`. On any error in either ping, log (include which DB failed if practical), call `await this.close()`, rethrow — preserve fail-fast behavior (CONTEXT D-01).
    Add a public getter `get chatDb(): Db` (or method `getChatDb(): Db`) returning the chat `Db`.
    Extend exported `IDatabaseService` to include the same chat accessor signature so callers typing against the interface see chat access.
    Do not add chat collections, chat index methods, or call chat from `initializeIndexes()`.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `src/database/mongodb/database.service.ts` contains `chatDatabaseName` in the constructor config type.
    - Same file contains `this.client.db(` twice (social + chat) OR equivalent two distinct `Db` fields from one client.
    - Same file contains `ping` against chat (e.g. `this.chatDb.command({ ping: 1 })` or `chatDb.command`).
    - Same file exports a `chatDb` getter or `getChatDb` method on the class and documents it on `IDatabaseService`.
    - `initializeIndexes()` body does not reference `chatDb` or `chatDatabaseName`.
  </acceptance_criteria>
  <done>Dual ping and chat Db exposed without chat indexes.</done>
</task>

<task type="auto">
  <name>Task 3: Bootstrap, types, and script wiring</name>
  <files>src/types/app.type.ts, src/index.ts, src/database/mongodb/index.ts, scripts/fake-data.ts</files>
  <read_first>src/types/app.type.ts, src/index.ts, src/database/mongodb/index.ts, scripts/fake-data.ts, src/app.ts</read_first>
  <action>
    In `AppConfig` (`src/types/app.type.ts`), extend `database` object type with `chatDatabaseName: string` alongside `uri` and `databaseName`.
    In `src/index.ts`, import `envConfig` (already available via config import path — use existing `@/config` pattern), set `chatDatabaseName: envConfig.DATABASE_CHAT_NAME` inside the object passed to `createApp` for `database`.
    In `DatabaseInstance.init` (`src/database/mongodb/index.ts`), change the `config` parameter type to require `chatDatabaseName: string` and pass it through to `new DatabaseService(config)`.
    In `scripts/fake-data.ts`, add `chatDatabaseName: envConfig.DATABASE_CHAT_NAME` to the `new DatabaseService({ ... })` argument object so the script compiles and connects consistently.
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - `src/types/app.type.ts` contains the substring `chatDatabaseName`.
    - `src/index.ts` contains `DATABASE_CHAT_NAME` in the database block passed to `createApp`.
    - `src/database/mongodb/index.ts` `init` accepts config including `chatDatabaseName`.
    - `scripts/fake-data.ts` passes `chatDatabaseName` into `DatabaseService` constructor.
    - Command `npm run build` exits with code 0.
  </acceptance_criteria>
  <done>App and fake-data script compile; runtime receives chat DB name from env.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` exits 0
- [ ] `rg "DATABASE_CHAT_NAME" src/config/envConfig.ts src/index.ts scripts/fake-data.ts` shows all three files
- [ ] Optional: `npm run dev` with valid `.env.development` shows log evidence both DBs connected (executor may add a single info log line in connect for chat DB name if not already present)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No chat indexes or collections created in Phase 1
- INFR-01 and INFR-03 addressed in code and example env
  </success_criteria>

<output>
After completion, create `.planning/phases/01-chat-database-foundation/01-01-SUMMARY.md`
</output>
