---
phase: 06-realtime-socket-io
plan: "03"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - src/services/socket.service.ts
  - src/container/index.ts
  - src/app.ts
  - .planning/phases/06-realtime-socket-io/06-SOCKET-CONTRACT.md
autonomous: true
requirements:
  - SOCK-02

must_haves:
  truths:
    - Mutual friends receive `presence:user` on connect/disconnect of a friend; last disconnect clears online (D-01).
    - `chat:typing` is ephemeral, throttled, membership-checked; no DB writes (D-12/D-13).
    - `notification:new` still works with multi-tab `user:<userId>` delivery; D-11 documented (no rename).
    - Subscribers to a chat receive `presence:chat` with `anyMemberOnline` after subscribe and when aggregate changes (D-03).
  artifacts:
    - path: src/services/socket.service.ts
      provides: typing + presence handlers
    - path: .planning/phases/06-realtime-socket-io/06-SOCKET-CONTRACT.md
      provides: Final D-11 + presence/typing notes matching code
  key_links:
    - from: socket connection
      to: FriendshipRepository.findFriendUserIdsForUser
      via: presence fan-out
    - from: chat:typing
      to: chat room emit
      via: SOCKET_CLIENT_CHAT_TYPING
---

## Objective

Complete **SOCK-02**: online presence per D-01–D-04, group aggregate **D-03**, typing **D-12/D-13**, and confirm **D-11** notification path on `user:<userId>` rooms (no event rename).

## Scope

In: `SocketService` (+ deps), container/app wiring, contract doc touch-ups.  
Out: New REST routes; Vitest harness (Phase 7 / VALIDATION wave 0).

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-realtime-socket-io/06-CONTEXT.md
@.planning/phases/06-realtime-socket-io/06-SOCKET-CONTRACT.md
@src/repositories/friendship.repository.ts
@src/repositories/chatMember.repository.ts

<interfaces>
From `src/repositories/friendship.repository.ts` (verify exact method name in file):
```typescript
// Use repository method that returns mutual friend ObjectIds for a user, e.g. findFriendUserIdsForUser(userId: ObjectId): Promise<ObjectId[]>
```
Executor: grep `findFriend` in `friendship.repository.ts` and call the existing public API from `SocketService`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Typing indicator + throttle</name>
  <files>src/services/socket.service.ts</files>
  <read_first>src/services/socket.service.ts, src/constants/socket.constant.ts, .planning/phases/06-realtime-socket-io/06-SOCKET-CONTRACT.md</read_first>
  <action>
    Handle `SOCKET_CLIENT_CHAT_TYPING` (`chat:typing`) with payload `{ conversationId: string, typing: boolean }`. Derive `userId` only from `socket.handshake.auth.decoded` (**SOCK-01**). Validate `ObjectId` for `conversationId`. `findMembership` must succeed else ignore. Broadcast to `chat:<conversationId>`: `this.io.to(chatRoom(conversationId)).emit('chat:typing', { conversationId, userId, typing })` — use same event name for client in/out per contract, or use a distinct server event if you split names; **keep contract:** server emits `chat:typing` with that payload. **D-13:** In-memory throttle per `(userId, conversationId)`: minimum **2000ms** between emits when `typing === true` (allow `typing: false` through immediately for “stop”). No repository calls beyond membership check. No DB writes.
  </action>
  <verify>
    <automated>cd /Users/dolong/Documents/NodeJS/nodejs-social && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd /Users/dolong/Documents/NodeJS/nodejs-social && npm run build` exits 0.
    - `grep -q "SOCKET_CLIENT_CHAT_TYPING\\|chat:typing" src/services/socket.service.ts`
    - `grep -q "findMembership" src/services/socket.service.ts`
  </acceptance_criteria>
  <done>Typing events are membership-gated, throttled, ephemeral.</done>
</task>

<task type="auto">
  <name>Task 2: Presence (friend + chat aggregate) + D-11 verification doc</name>
  <files>src/services/socket.service.ts, src/container/index.ts, src/app.ts, .planning/phases/06-realtime-socket-io/06-SOCKET-CONTRACT.md</files>
  <read_first>src/services/socket.service.ts, src/repositories/friendship.repository.ts, src/repositories/chatMember.repository.ts, src/constants/socket.constant.ts, src/services/notifications.service.ts</read_first>
  <action>
    **Deps:** Inject `IFriendshipRepository` (or concrete used in container) into `SocketService` — add `getFriendshipRepository()` on `IContainer` + `app.ts` wiring alongside chat member repo.
    **Friend presence D-01/D-02:** On connection, after `user` room join, `const friends = await friendshipRepository.findFriendUserIdsForUser(new ObjectId(userId))` (use actual method signature from codebase). For each `friendOid`, `this.io.to(userRoom(friendOid.toHexString())).emit(SOCKET_SERVER_PRESENCE_USER, { userId: userId, online: true })`. On `disconnect`, `const remaining = await this.io.in(userRoom(userId)).fetchSockets()` — if `remaining.length === 0`, emit same event with `online: false` to each friend’s `user:` room. **D-04** already enforced at handshake.
    **Chat aggregate D-03:** Add private helper `emitChatPresenceAggregate(chatIdHex: string)` using `listMembers` + for each member check ` (await this.io.in(userRoom(memberHex)).fetchSockets()).length > 0 `, set `anyMemberOnline = any true`, then `this.io.to(chatRoom(chatIdHex)).emit(SOCKET_SERVER_PRESENCE_CHAT, { conversationId: chatIdHex, anyMemberOnline })`. Call it after successful `chat:subscribe` join. On disconnect, for each room in `socket.rooms` where name starts with `SOCKET_ROOM_CHAT_PREFIX`, extract id and call `emitChatPresenceAggregate`. On connect, optionally skip global chat updates (only on subscribe/disconnect).
    **D-11:** In `06-SOCKET-CONTRACT.md`, add one line under D-11: `NOTIFICATION_SOCKET_EVENT` in `src/constants/notification.constant.ts` remains `'notification:new'`; `NotificationsService` uses `emitToUser` → `user:<userId>` room (plan 01). No client rename.
  </action>
  <verify>
    <automated>cd /Users/dolong/Documents/NodeJS/nodejs-social && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd /Users/dolong/Documents/NodeJS/nodejs-social && npm run build` exits 0.
    - `grep -q "SOCKET_SERVER_PRESENCE_USER" src/services/socket.service.ts`
    - `grep -q "SOCKET_SERVER_PRESENCE_CHAT" src/services/socket.service.ts`
    - `grep -q "fetchSockets" src/services/socket.service.ts`
    - `grep -q "getFriendshipRepository" src/container/index.ts src/app.ts`
    - `grep -q "notification:new" .planning/phases/06-realtime-socket-io/06-SOCKET-CONTRACT.md src/constants/notification.constant.ts`
  </acceptance_criteria>
  <done>Presence and chat aggregate implemented; contract documents D-11 retention.</done>
</task>

</tasks>

## Verification

Build passes; grep confirms presence events and friendship wiring.

## Success criteria

SOCK-02 presence/typing satisfied; notifications remain persist-then-emit with multi-tab delivery.

## Output

After completion, create `.planning/phases/06-realtime-socket-io/06-03-SUMMARY.md`.
