---
phase: 06-realtime-socket-io
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/constants/socket.constant.ts
  - src/services/socket.service.ts
  - src/container/index.ts
  - src/app.ts
  - .planning/phases/06-realtime-socket-io/06-SOCKET-CONTRACT.md
autonomous: true
requirements:
  - SOCK-01
  - SOCK-02

must_haves:
  truths:
    - Socket identity comes only from verified JWT at handshake and refreshed packet verify; no client `senderId` path remains.
    - Each connection joins `user:<userId>`; `emitToUser` fans out to that room (multi-tab D-05).
    - Client can subscribe to `chat:<conversationId>` only after server membership check; legacy `sendMessage`/`receiveMessage` removed.
  artifacts:
    - path: src/constants/socket.constant.ts
      provides: Room prefixes, server/client event name constants
    - path: src/services/socket.service.ts
      provides: join user room, chat:subscribe/unsubscribe, emitToUser via rooms
    - path: src/container/index.ts
      provides: getChatMemberRepository() for SocketService wiring
  key_links:
    - from: src/app.ts
      to: SocketService constructor
      via: pass IChatMemberRepository from container
    - from: socket.service.ts
      to: IChatMemberRepository.findMembership
      via: chat:subscribe handler
---

## Objective

Establish Socket.IO foundations: **SOCK-01** (JWT-only identity, remove trusted-client chat events) and **SOCK-02** partial (per-user room `user:<userId>`, conversation room `chat:<conversationId>` after membership check).

## Scope

In: constants, `SocketService` refactor, container getter + `app.ts` wiring, contract doc path listed (align with `06-SOCKET-CONTRACT.md`).  
Out: message/read emits (plan 02), presence/typing (plan 03).

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-realtime-socket-io/06-CONTEXT.md
@.planning/phases/06-realtime-socket-io/06-RESEARCH.md
@.planning/phases/06-realtime-socket-io/06-SOCKET-CONTRACT.md
@src/services/socket.service.ts
@src/repositories/chatMember.repository.ts
@src/container/index.ts
@src/app.ts

<interfaces>
From `src/repositories/chatMember.repository.ts`:
```typescript
export interface IChatMemberRepository {
  findMembership(conversationId: ObjectId, userId: ObjectId): Promise<IChatMember | null>;
}
```
From `src/services/socket.service.ts`:
```typescript
export interface ISocketService {
  run(): void;
  emitToUser(userId: string, event: string, data: unknown): void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Socket constants + contract alignment</name>
  <files>src/constants/socket.constant.ts, .planning/phases/06-realtime-socket-io/06-SOCKET-CONTRACT.md</files>
  <read_first>src/constants/notification.constant.ts, .planning/phases/06-realtime-socket-io/06-SOCKET-CONTRACT.md, .planning/phases/06-realtime-socket-io/06-CONTEXT.md</read_first>
  <action>
    Add `src/constants/socket.constant.ts` exporting exactly (adjust names only if you add helpers, but keep string values):
    - `SOCKET_ROOM_USER_PREFIX = 'user:'`, `SOCKET_ROOM_CHAT_PREFIX = 'chat:'`
    - Helpers `userRoom(userId: string)` → `` `${SOCKET_ROOM_USER_PREFIX}${userId}` ``, `chatRoom(conversationId: string)` → `` `${SOCKET_ROOM_CHAT_PREFIX}${conversationId}` ``
    - Client events: `SOCKET_CLIENT_CHAT_SUBSCRIBE = 'chat:subscribe'`, `SOCKET_CLIENT_CHAT_UNSUBSCRIBE = 'chat:unsubscribe'`, `SOCKET_CLIENT_CHAT_TYPING = 'chat:typing'` (typing used in plan 03)
    - Server events: `SOCKET_SERVER_CHAT_MESSAGE_NEW = 'chat:message:new'`, `SOCKET_SERVER_CHAT_READ_UPDATED = 'chat:read:updated'`, `SOCKET_SERVER_PRESENCE_USER = 'presence:user'`, `SOCKET_SERVER_PRESENCE_CHAT = 'presence:chat'`
    Ensure `06-SOCKET-CONTRACT.md` table event names match these literals (edit contract if drift). **D-09** single message event name = `chat:message:new`. **D-11:** contract already states keep `notification:new` — do not rename.
  </action>
  <verify>
    <automated>cd /Users/dolong/Documents/NodeJS/nodejs-social && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd /Users/dolong/Documents/NodeJS/nodejs-social && npm run build` exits 0.
    - `grep -q "SOCKET_ROOM_USER_PREFIX = 'user:'" src/constants/socket.constant.ts`
    - `grep -q "chat:subscribe" src/constants/socket.constant.ts`
    - `grep -q "chat:message:new" src/constants/socket.constant.ts`
    - `grep -q "chat:message:new" .planning/phases/06-realtime-socket-io/06-SOCKET-CONTRACT.md`
  </acceptance_criteria>
  <done>Constants file exists; contract and code use the same event strings.</done>
</task>

<task type="auto">
  <name>Task 2: SocketService rooms, subscribe, remove legacy map</name>
  <files>src/services/socket.service.ts, src/container/index.ts, src/app.ts</files>
  <read_first>src/services/socket.service.ts, src/container/index.ts, src/app.ts, src/repositories/chatMember.repository.ts, src/constants/socket.constant.ts</read_first>
  <action>
    **SOCK-01 / D-08:** Delete handlers for `sendMessage` and `receiveMessage` entirely (no replacement socket send path). **D-06** send only via REST.
    **D-05 / SOCK-02:** Remove `private users: Map<string, string>`. On `connection`, `socket.join(userRoom(userId))` using `userId` from `socket.handshake.auth.decoded` only (`TokenPayload`).
    **emitToUser(userId, event, data):** implement as `this.io.to(userRoom(userId)).emit(event, data)` (import `userRoom` from constants).
    **Packet middleware:** after successful `verifyAccessToken`, set `socket.handshake.auth.decoded = decoded` and `socket.handshake.auth.accessToken = accessToken` so identity stays fresh (pitfall from RESEARCH).
    **disconnect:** remove map delete; optional `socket.leave` is automatic on disconnect.
    **chat:subscribe / chat:unsubscribe:** listen using `SOCKET_CLIENT_*` constants. Payload `{ conversationId: string }`. Parse `ObjectId` safely; on invalid id ignore or ack error (choose one consistent pattern). Call `this.chatMemberRepository.findMembership(cid, viewerOid)`; if null, do not join. If ok, `socket.join(chatRoom(conversationId))` / `leave`.
    **Container:** add `getChatMemberRepository(): IChatMemberRepository` to `IContainer` and public implementation returning `this.chatMemberRepository`.
    **app.ts:** `new SocketService(httpServer, container.getUsersService(), container.getChatMemberRepository())` — extend `SocketService` constructor to accept `IChatMemberRepository` as third argument.
    Keep existing handshake checks (verified/banned) — **D-04**.
  </action>
  <verify>
    <automated>cd /Users/dolong/Documents/NodeJS/nodejs-social && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd /Users/dolong/Documents/NodeJS/nodejs-social && npm run build` exits 0.
    - `! grep -q "sendMessage" src/services/socket.service.ts` (no legacy listener name)
    - `! grep -q "receiveMessage" src/services/socket.service.ts`
    - `! grep -q "Map<string, string>" src/services/socket.service.ts`
    - `grep -q "userRoom" src/services/socket.service.ts`
    - `grep -q "chat:subscribe\\|SOCKET_CLIENT_CHAT_SUBSCRIBE" src/services/socket.service.ts`
    - `grep -q "getChatMemberRepository" src/container/index.ts src/app.ts`
  </acceptance_criteria>
  <done>JWT-only connection path; per-user room fan-out; membership-gated chat rooms; legacy chat socket API removed.</done>
</task>

</tasks>

## Verification

Build passes; grep confirms removal of legacy events and map; subscribe uses `findMembership`.

## Success criteria

SOCK-01 and SOCK-02 room/join foundations satisfied; `NotificationsService` continues to call `emitToUser` but delivery now hits all tabs in `user:<userId>`.

## Output

After completion, create `.planning/phases/06-realtime-socket-io/06-01-SUMMARY.md`.
