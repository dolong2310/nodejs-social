---
phase: 06-realtime-socket-io
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/ports/realtimeChatEmitter.port.ts
  - src/services/socket.service.ts
  - src/services/chatMessages.service.ts
  - src/container/index.ts
autonomous: true
requirements:
  - SOCK-03

must_haves:
  truths:
    - After `insertMessage` + `touchUpdatedAt` succeed in `ChatMessagesService.sendMessage`, server emits realtime with full HTTP message DTO; not before DB.
    - After `updateReadState` succeeds in `markRead`, server emits read receipt to conversation + user tabs per D-07.
  artifacts:
    - path: src/ports/realtimeChatEmitter.port.ts
      provides: Narrow emitter interface for chat realtime
    - path: src/services/chatMessages.service.ts
      provides: Calls emitter only after successful persistence
    - path: src/services/socket.service.ts
      implements: Emitter methods using `io.to(chatRoom).to(userRoom)...` or equivalent union broadcast
  key_links:
    - from: ChatMessagesService.sendMessage
      to: insertMessage
      via: emit after return dto path
    - from: ChatMessagesService.markRead
      to: updateReadState
      via: emit after await succeeds
---

## Objective

Implement **SOCK-03** persist-then-emit for chat messages and read state, reusing HTTP shapes (**D-10**, **D-07**).

## Scope

In: new port interface, `SocketService` emit helpers, `ChatMessagesService` hooks, container wiring for `ChatMessagesService` constructor.  
Out: presence/typing (plan 03).

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-realtime-socket-io/06-CONTEXT.md
@.planning/phases/06-realtime-socket-io/06-SOCKET-CONTRACT.md
@src/services/chatMessages.service.ts
@src/dtos/responses/chatMessage.response.dto.ts
@src/constants/socket.constant.ts

<interfaces>
From `src/dtos/responses/chatMessage.response.dto.ts`:
```typescript
export interface ChatMessageResponseDTO {
  id: string;
  conversationId: string;
  senderId: string;
  text?: string;
  attachments?: IChatAttachment[];
  createdAt: string;
}
export function toChatMessageDto(m: IChatMessage): ChatMessageResponseDTO;
```
From `src/services/chatMessages.service.ts`:
```typescript
export interface IChatMessagesService {
  sendMessage(userId: string, conversationId: string, body: SendChatMessageBodyDTO): Promise<ChatMessageResponseDTO>;
  markRead(userId: string, conversationId: string, body: MarkChatReadBodyDTO): Promise<void>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: realtimeChat port + SocketService implementation</name>
  <files>src/ports/realtimeChatEmitter.port.ts, src/services/socket.service.ts, src/container/index.ts</files>
  <read_first>src/services/socket.service.ts, src/constants/socket.constant.ts, src/container/index.ts, .planning/phases/06-realtime-socket-io/06-SOCKET-CONTRACT.md</read_first>
  <action>
    **Single pattern (mirror Phase 5 `NotificationsService.bindSocketEmitter`):**
    1. Add `src/ports/realtimeChatEmitter.port.ts` exporting `IRealtimeChatEmitter` with:
       - `emitMessageCreated(chatIdHex: string, memberUserIdHexes: string[], message: ChatMessageResponseDTO): void`
       - `emitReadUpdated(chatIdHex: string, viewerUserIdHex: string, payload: { lastReadMessageId: string; lastReadAt: string }): void`
    2. On `SocketService`, implement `IRealtimeChatEmitter`:
       - **Message:** `this.io.to([chatRoom(chatIdHex), ...memberUserIdHexes.map(userRoom)]).emit(SOCKET_SERVER_CHAT_MESSAGE_NEW, { message })` (Socket.IO v4 `to(array)` = union of rooms).
       - **Read:** same union for `SOCKET_SERVER_CHAT_READ_UPDATED` with `{ conversationId: chatIdHex, userId: viewerUserIdHex, lastReadMessageId, lastReadAt }`.
    3. On `ChatMessagesService`: private `realtimeChatEmitter: IRealtimeChatEmitter | null = null` and `bindRealtimeChatEmitter(emitter: IRealtimeChatEmitter | null): void { this.realtimeChatEmitter = emitter; }` (same lifecycle as notifications emitter).
    4. On `IContainer` + `Container`: `bindRealtimeChatEmitter(emitter: IRealtimeChatEmitter | null): void` that calls `this.chatMessagesService.bindRealtimeChatEmitter(emitter)`.
    5. Do **not** rebuild `ChatMessagesService` in the container when binding; constructor stays unchanged from Phase 4/5 deps.
  </action>
  <verify>
    <automated>cd /Users/dolong/Documents/NodeJS/nodejs-social && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd /Users/dolong/Documents/NodeJS/nodejs-social && npm run build` exits 0.
    - `grep -q "IRealtimeChatEmitter" src/ports/realtimeChatEmitter.port.ts`
    - `grep -q "SOCKET_SERVER_CHAT_MESSAGE_NEW" src/services/socket.service.ts`
    - `grep -q "SOCKET_SERVER_CHAT_READ_UPDATED" src/services/socket.service.ts`
    - `grep -q "bindRealtimeChatEmitter" src/container/index.ts`
  </acceptance_criteria>
  <done>Emitter interface exists; SocketService emits named events with documented payloads.</done>
</task>

<task type="auto">
  <name>Task 2: ChatMessagesService persist-then-emit hooks</name>
  <files>src/services/chatMessages.service.ts, src/container/index.ts, src/app.ts</files>
  <read_first>src/services/chatMessages.service.ts, src/container/index.ts, src/app.ts, src/ports/realtimeChatEmitter.port.ts, src/repositories/chatMember.repository.ts</read_first>
  <action>
    **sendMessage:** after `await this.chatMessageRepository.insertMessage(msg)` and `await this.chatRepository.touchUpdatedAt(...)` succeed, before notifications block, compute `const dto = toChatMessageDto(msg)`; `await this.chatMemberRepository.listMembers(cid)` → `memberHexes` = all `userId.toHexString()`; if `this.realtimeChatEmitter` non-null, `emitMessageCreated(chatIdHex, memberHexes, dto)`. Keep existing `recordNewMessage` notification logic unchanged.
    **markRead:** after `await this.chatMemberRepository.updateReadState(...)` succeeds, if `this.realtimeChatEmitter` non-null, `listMembers` → same `memberHexes`, `emitReadUpdated(chatIdHex, userId, { lastReadMessageId: messageId.toHexString(), lastReadAt: at.toISOString() })`. Early-return when no messages: **no emit**. **SOCK-03:** never emit if persistence throws or skips update.
    **app.ts:** after `new SocketService(...)` and before `socket.run()`, call `container.bindRealtimeChatEmitter(socketService)` (same placement as `bindNotificationsSocket`).
  </action>
  <verify>
    <automated>cd /Users/dolong/Documents/NodeJS/nodejs-social && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd /Users/dolong/Documents/NodeJS/nodejs-social && npm run build` exits 0.
    - `grep -q "emitMessageCreated" src/services/chatMessages.service.ts`
    - `grep -q "emitReadUpdated" src/services/chatMessages.service.ts`
    - `grep -q "listMembers" src/services/chatMessages.service.ts`
    - `grep -q "bindRealtimeChatEmitter" src/services/chatMessages.service.ts`
    - `grep -q "bindRealtimeChatEmitter" src/app.ts`
  </acceptance_criteria>
  <done>Message and read realtime only after DB success; payload uses `toChatMessageDto` for messages.</done>
</task>

</tasks>

## Verification

Build passes; emit calls sit strictly after awaited persistence lines.

## Success criteria

SOCK-03 satisfied for chat send and read; contract `chat:message:new` and `chat:read:updated` documented shapes match implementation.

## Output

After completion, create `.planning/phases/06-realtime-socket-io/06-02-SUMMARY.md`.
