---
phase: 03-posts-feed-engagement
plan: "04"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/repositories/search.repository.ts
  - src/services/search.service.ts
  - src/container/index.ts
  - scripts/fake-data.ts
  - swagger/paths.yaml
  - swagger/components.yaml

autonomous: true
requirements:
  - POST-01
  - FEED-01
  - BLCK-02

must_haves:
  truths:
    - Post search / discovery queries use the same **audience literals** as posts (`public`, `friends-only`, `only-me`) and **exclude posts authored by users blocked with the searcher** when `userId` is present (BLCK-02).
    - Fake seed data creates posts with **`allowStrangerComments`** and new audience enum values so local dev matches Phase 3 contracts.
  artifacts:
    - path: src/repositories/search.repository.ts
      provides: _getPostMatch uses FRIENDS_ONLY + block-aware conditions for authenticated users
    - path: scripts/fake-data.ts
      provides: CreatePostRequestDTO-shaped bodies include allowStrangerComments and new audience enum
  key_links:
    - from: SearchRepository._getPostMatch
      to: listUserIdsBlockedInEitherDirection
      via: SearchService or repository receives blocked ids (same pattern as PostsService plan `02`)
---

## Objective

Align **search post match** and **dev seed data** with Phase 3 visibility + **BLCK-02**, and apply **minimal Swagger** updates so audience / `allowStrangerComments` / new routes are not wildly wrong (D-08 — full polish deferred). Depends on plan `01` enum/DTO literals.

## Scope

- In: `search.repository.ts` (+ `search.service.ts` / `Container` if block ids must be fetched in service layer), `fake-data.ts`, `swagger/paths.yaml` and `components.yaml` as needed for post create body + PATCH + likes paths.
- Out: Comprehensive OpenAPI redesign; ranking; any new search features.

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-posts-feed-engagement/03-CONTEXT.md
@src/repositories/search.repository.ts
@src/services/search.service.ts
@scripts/fake-data.ts
@swagger/paths.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Search repository + service — audience literals and BLCK-02 for authenticated search</name>
  <files>src/repositories/search.repository.ts, src/services/search.service.ts, src/container/index.ts</files>
  <read_first>
    src/repositories/search.repository.ts
    src/services/search.service.ts
    src/repositories/block.repository.ts
    .planning/phases/03-posts-feed-engagement/03-02-PLAN.md
  </read_first>
  <action>
    Replace **`EPostAudience.FOLLOWERS`** with **`FRIENDS_ONLY`** and old string `only_me` with **`only-me`** throughout `_getPostMatch`.

    For **authenticated** `userId` present: extend match so posts from authors in **`listUserIdsBlockedInEitherDirection`** are excluded (same semantic as feed BLCK-02). Implement by injecting **`BlockRepository`** into `SearchService`, resolving blocked ids before `findPosts` / `countPosts`, and passing **`blockedAuthorIds`** into repository methods (mirror plan `02` pattern — service fetches blocks, repository applies `$nin`).

    Preserve **guest / no userId** behavior: public-only or existing guest semantics; **no** block list.

    Ensure `$or` visibility branches for logged-in users align with **FEED-01** intent (public + eligible friends-only + own only-me) **within text search**, not the full merged feed algorithm if prohibitively complex — **minimum:** apply **block exclusion** on all returned posts and fix enum literals; **ideal:** match feed eligibility per ENGA-01.

    **GitNexus:** Impact analysis on `SearchRepository` / `SearchService` before edits.
  </action>
  <verify>
    <automated>cd /Users/dolong/Documents/NodeJS/nodejs-social && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "FOLLOWERS|'followers'|only_me" src/repositories/search.repository.ts` returns no matches in match logic (comments may mention migration only).
    - `rg "blockedAuthorIds|listUserIdsBlockedInEitherDirection" src/repositories/search.repository.ts src/services/search.service.ts` shows block integration for authenticated path.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Search post results respect new audience strings and BLCK-02 for logged-in users.</done>
</task>

<task type="auto">
  <name>Task 2: fake-data + minimal Swagger touchpoints</name>
  <files>scripts/fake-data.ts, swagger/paths.yaml, swagger/components.yaml</files>
  <read_first>
    scripts/fake-data.ts
    swagger/paths.yaml
    swagger/components.yaml
  </read_first>
  <action>
    **fake-data.ts:** Update `createRandomPostBody` to use `EPostAudience` after plan `01` (FRIENDS_ONLY, ONLY_ME). Add **`allowStrangerComments: faker.datatype.boolean()`** (or weighted true) to satisfy create validation. Remove comments referring to `followers` enum.

    **Swagger (D-08 minimal):** Update post schemas to list audience enum `public`, `friends-only`, `only-me`; add `allowStrangerComments` boolean on create/Patch where post request bodies are defined. If plan `03` added **PATCH /posts/{postId}** and **likes** routes, add stub paths or extend components so `/docs` does not contradict reality — **full** response examples optional.

    Do **not** expand scope to unrelated tags.
  </action>
  <verify>
    <automated>cd /Users/dolong/Documents/NodeJS/nodejs-social && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "allowStrangerComments" scripts/fake-data.ts` returns a match.
    - `rg "friends-only|only-me" swagger/paths.yaml swagger/components.yaml` has at least one match for updated audience docs.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Local seed and Swagger minimally reflect Phase 3 post contracts.</done>
</task>

</tasks>

## verification_criteria

- `npm run build` and `npm run lint` (if lint is fast) succeed.
- Optional: `npx tsx scripts/fake-data.ts --env=development` runs without validation errors after code changes (note in SUMMARY if env-dependent).

## success_criteria

- POST-01 literals propagate to search + seeds; FEED-01 / BLCK-02 alignment for authenticated search; dev UX unbroken.

<output>
After completion, create `.planning/phases/03-posts-feed-engagement/03-04-SUMMARY.md`
</output>
