---
phase: 03-posts-feed-engagement
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/repositories/post.repository.ts
  - src/services/posts.service.ts
  - src/controllers/posts.controller.ts
  - src/container/index.ts
autonomous: true
requirements:
  - FEED-01
  - FEED-02
  - BLCK-02

must_haves:
  truths:
    - Authenticated home feed includes **system-wide `public`** posts (from authors not blocked with viewer per BLCK-02) **and** friends’ **`friends-only`** posts (mutual friends), **and** viewer’s own posts for all audiences including `only-me`; merged and sorted by `createdAt` descending (FEED-01, FEED-02).
    - Guest home feed returns **only** `public` posts; block pairs do not apply (no viewer user id) per D-03 — **do not** filter guest feed by `blocks` collection.
    - Every feed query for an **authenticated** viewer excludes posts whose **author** is in `listUserIdsBlockedInEitherDirection(viewerId)` (BLCK-02).
  artifacts:
    - path: src/repositories/post.repository.ts
      provides: findPosts / countPosts (and optionally guest variants) implementing visibility + block author exclusion
    - path: src/services/posts.service.ts
      provides: resolves blocked id list + friend ids then calls repository
  key_links:
    - from: PostsController.getNewFeeds
      to: BlockRepository.listUserIdsBlockedInEitherDirection
      via: PostsService passes blocked ObjectIds into feed query
    - from: PostRepository feed match
      to: EPostAudience.FRIENDS_ONLY
      via: $or branch for friends-only from friend userIds only
---

## Objective

Implement **merged chronological feed** with **BLCK-02** author exclusion for authenticated users, and **guest feed** = public-only (D-03). Replace obsolete “followers-only feed slice” logic with **mutual friends** + **global public** semantics (FEED-01, FEED-02). Depends on plan `03-01` audience enum literals.

## Scope

- In: `PostRepository` feed `match` construction, `PostsService` constructor deps (`IBlockRepository` or `BlockRepository`), `getNewFeeds` / `getGuestNewFeeds` wiring, `Container` registration, `PostsController` only if service signature changes.
- Out: `audienceValidation` / optional auth (plan `03-03`), search (plan `04`), engagement routes.

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-posts-feed-engagement/03-CONTEXT.md
@.planning/phases/02-friends-graph-privacy/02-CONTEXT.md
@src/repositories/post.repository.ts
@src/repositories/block.repository.ts
@src/services/posts.service.ts
@src/controllers/posts.controller.ts
@src/utils/posts.pipeline.util.ts

<interfaces>
From `src/repositories/block.repository.ts`:

```typescript
async listUserIdsBlockedInEitherDirection(viewerId: ObjectId): Promise<ObjectId[]>;
```

From `src/services/friends.service.ts`:

```typescript
async findFollowedUserIds(userId: string): Promise<ObjectId[]>; // mutual friend ObjectIds
```

From `buildBasePostPipeline` — preserve existing sort if it already uses `createdAt: -1`; fix if not (FEED-02).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PostRepository — feed match for auth + guest with BLCK-02 (auth only)</name>
  <files>src/repositories/post.repository.ts</files>
  <read_first>
    src/repositories/post.repository.ts
    src/utils/posts.pipeline.util.ts
    .planning/phases/03-posts-feed-engagement/03-CONTEXT.md
  </read_first>
  <action>
    Extend `findPosts` / `countPosts` payloads to accept **`blockedAuthorIds: ObjectId[]`** (may be empty). For **authenticated** feed, build `$match` equivalent to:

    - Global branch: `{ audience: 'public', userId: { $nin: blockedAuthorIds } }`.
    - Own posts branch: `{ userId: viewerObjectId }` (viewer always sees own posts regardless of audience; still not blocked against self — blocked list should never contain self; assert or filter self out defensively).
    - Friends-only branch: `{ audience: 'friends-only', userId: { $in: friendIds, $nin: blockedAuthorIds } }` where `friendIds` is the mutual friends list from service (exclude viewer id if present).

    Remove logic that **only** shows posts from followed users when the friends list is non-empty — public posts from **non-friends** must appear (FEED-01).

    **Guest** `findGuestPosts` / `countGuestPosts`: keep `audience: 'public'` only; **no** block filter.

    Use `EPostAudience` enum members after plan `01` (`FRIENDS_ONLY`, etc.) in match conditions.

    Ensure aggregation ends with **createdAt descending** total ordering (FEED-02) — adjust `buildBasePostPipeline` or final `$sort` if current pipeline sorts correctly already.

    **GitNexus:** Run `gitnexus_impact` on changed repository methods before editing.
  </action>
  <verify>
    <automated>cd /Users/dolong/Documents/NodeJS/nodejs-social && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "blockedAuthorIds|blocked" src/repositories/post.repository.ts` shows the new parameter used in match for authenticated paths.
    - `rg "FOLLOWERS|'followers'" src/repositories/post.repository.ts` returns no matches (use `FRIENDS_ONLY` / `friends-only`).
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Repository encodes FEED-01 + FEED-02 + BLCK-02 for auth; guest remains public-only.</done>
</task>

<task type="auto">
  <name>Task 2: PostsService + Container — load blocked ids and pass into feed queries</name>
  <files>src/services/posts.service.ts, src/container/index.ts, src/controllers/posts.controller.ts</files>
  <read_first>
    src/services/posts.service.ts
    src/container/index.ts
    src/repositories/block.repository.ts
  </read_first>
  <action>
    Inject **`BlockRepository`** (or `IBlockRepository` interface if project prefers) into `PostsService` alongside `PostRepository`.

    In **`getNewFeeds`**: for `userId`, compute `blockedAuthorIds = await blockRepository.listUserIdsBlockedInEitherDirection(new ObjectId(userId))`, then pass to `findPosts` / `countPosts` with existing `followedUserIds` from controller (friend list).

    **`getGuestNewFeeds`:** unchanged — no block list.

    Update **`Container`** factory for `PostsService` to pass `BlockRepository` instance.

    **`PostsController`:** unchanged call shape if service still accepts same payload; if service now computes blocks internally, remove redundant controller logic — prefer service-owned block fetch to keep controller thin.

    Ensure **TypeScript** `IPostsService` / constructor signatures updated everywhere `PostsService` is constructed.
  </action>
  <verify>
    <automated>cd /Users/dolong/Documents/NodeJS/nodejs-social && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "listUserIdsBlockedInEitherDirection|BlockRepository" src/services/posts.service.ts` has at least one match for the block list call.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Authenticated feed calls include BLCK-02 author exclusion end-to-end from service layer.</done>
</task>

</tasks>

## verification_criteria

- Manual sanity: authenticated user with a block does not see blocked user’s `public` posts in `GET /posts` (document in SUMMARY).
- `npm run build` passes.

## success_criteria

- FEED-01, FEED-02, and BLCK-02 (read path for home feed) are satisfied for authenticated and guest behaviors per CONTEXT.

<output>
After completion, create `.planning/phases/03-posts-feed-engagement/03-02-SUMMARY.md`
</output>
