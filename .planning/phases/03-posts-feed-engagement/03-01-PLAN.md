---
phase: 03-posts-feed-engagement
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/enums/posts.enum.ts
  - src/models/schemas/post.schema.ts
  - src/dtos/requests/post.request.dto.ts
  - src/validations/posts.validation.ts
  - src/repositories/post.repository.ts
  - src/services/posts.service.ts
  - src/controllers/posts.controller.ts
  - src/routes/posts.route.ts
  - src/container/index.ts
autonomous: true
requirements:
  - POST-01
  - POST-02
  - POST-03

must_haves:
  truths:
    - API and persistence use audience literals `public`, `friends-only`, `only-me` (D-01); legacy `followers` / `only_me` strings are not written for new data.
    - Create post body **requires** `audience` and `allowStrangerComments` (D-06). POST-02 defaults are satisfied when clients send `public` + `true` as the normal case; service may coerce only if validation explicitly documents optional fields — **prefer required fields with documented defaults in API docs** (POST-02).
    - Author can update `audience` and `allowStrangerComments` via PATCH with both fields present in body (D-06, POST-03).
  artifacts:
    - path: src/enums/posts.enum.ts
      provides: EPostAudience values public | friends-only | only-me
    - path: src/models/schemas/post.schema.ts
      provides: IPost.allowStrangerComments boolean on documents
    - path: src/routes/posts.route.ts
      provides: PATCH /:postId for post owner updates
  key_links:
    - from: CreatePostRequestDTO
      to: PostEntity
      via: createPost repository insert includes allowStrangerComments
---

## Objective

Align **post persistence and write API** with Phase 3 decisions (D-01, D-02, D-06): new audience literals, `allowStrangerComments` on documents, required create fields, and **owner PATCH** for `audience` + `allowStrangerComments` (POST-01 — POST-03). **Out of scope for this plan:** feed queries, block filtering, guest vs unverified read paths, engagement endpoints beyond post create/PATCH — those are `03-02` / `03-03`.

## Scope

- In: enum, `IPost` / `PostEntity`, DTOs, `createPostValidation`, `PostRepository.createPost` + new `updatePostByOwner` (or equivalent), `PostsService`, `PostsController`, `PostsRoute` PATCH, `Container` wiring if new constructor deps (none expected if block/friends not used here).
- Out: BLCK-02, feed, search, bookmarks/likes, Swagger polish (plan `04`), changing `optionalAuth` behavior (plan `03`).

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-posts-feed-engagement/03-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/enums/posts.enum.ts
@src/models/schemas/post.schema.ts
@src/repositories/post.repository.ts
@src/validations/posts.validation.ts
@src/dtos/requests/post.request.dto.ts

<interfaces>
From `src/repositories/post.repository.ts` (existing patterns):

```typescript
export interface IPostRepository {
  findById(id: string): Promise<PostDetailResponseDTO>;
  findPostById(postId: string): Promise<IPost | null>;
  createPost(payload: { userId: string; body: Omit<CreatePostRequestDTO, 'hashtags'> & { hashtags: ObjectId[] } }): Promise<IPost>;
  // Plan adds: updatePostAudienceAndStrangerComments(postId: string, ownerUserId: string, patch: { audience: EPostAudience; allowStrangerComments: boolean }): Promise<IPost | null>;
}
```

From `CreatePostRequestDTO` — extend with `allowStrangerComments: boolean` (constructor + class field).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enum + schema + DTO — audience literals and allowStrangerComments</name>
  <files>src/enums/posts.enum.ts, src/models/schemas/post.schema.ts, src/dtos/requests/post.request.dto.ts</files>
  <read_first>
    .planning/phases/03-posts-feed-engagement/03-CONTEXT.md
    src/enums/posts.enum.ts
    src/models/schemas/post.schema.ts
  </read_first>
  <action>
    **D-01 / POST-01:** Replace `EPostAudience` with exactly three string values used in API and Mongo: `PUBLIC = 'public'`, `FRIENDS_ONLY = 'friends-only'`, `ONLY_ME = 'only-me'`. Remove `FOLLOWERS = 'followers'` and `ONLY_ME = 'only_me'`.

    **IPost / PostEntity:** Add `allowStrangerComments: boolean`. Constructor must set it; for backward reads, default `true` when loading old docs missing the field (application-level default in mapper or schema constructor when reading from DB — document in code comment that fresh DB is assumed per CONTEXT).

    Update the **commented** JSON-schema block at bottom of `post.schema.ts` to list `public`, `friends-only`, `only-me` and include `allowStrangerComments`.

    **CreatePostRequestDTO:** Add required `allowStrangerComments: boolean` to class + constructor.

    Add **UpdatePostRequestDTO** (or `PatchPostRequestDTO`) with **both** `audience: EPostAudience` and `allowStrangerComments: boolean` (D-06 — PATCH body must carry both explicitly).
  </action>
  <verify>
    <automated>cd /Users/dolong/Documents/NodeJS/nodejs-social && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "friends-only|only-me" src/enums/posts.enum.ts` matches the new literals; `rg "'followers'|'only_me'" src/enums/posts.enum.ts` returns no matches (unless only in comments explaining migration).
    - `rg "allowStrangerComments" src/models/schemas/post.schema.ts src/dtos/requests/post.request.dto.ts` returns matches in each file.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Types and DTOs match D-01 and carry `allowStrangerComments` for create and PATCH contracts.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Validation + repository + service — create defaults and owner PATCH</name>
  <files>src/validations/posts.validation.ts, src/repositories/post.repository.ts, src/services/posts.service.ts, src/controllers/posts.controller.ts, src/routes/posts.route.ts, src/container/index.ts</files>
  <read_first>
    src/validations/posts.validation.ts
    src/repositories/post.repository.ts
    src/routes/posts.route.ts
  </read_first>
  <action>
    **createPostValidation (D-06, POST-02):**
    - Require `allowStrangerComments` as boolean in body (use `isBoolean` or equivalent express-validator check).
    - Audience `isIn` must allow only `EPostAudience.PUBLIC | FRIENDS_ONLY | ONLY_ME`.
    - **POST-02:** If product requires default public when omitted, prefer requiring `audience` explicitly; if optional, default in service to `public` and `allowStrangerComments` to `true` — **locked:** CONTEXT requires field on create; keep **both required** in validation (no omit).

    **PostRepository:** Implement `updatePostAudienceAndStrangerComments` (name may vary): `updateOne` on `{ _id, userId: ownerObjectId }` with `$set: { audience, allowStrangerComments, updatedAt }`, return updated doc or boolean success.

    **PostsService:** `createPost` passes `allowStrangerComments` into repository body. Add `updateMyPost` / `patchPost` calling repository after ownership check.

    **PostsController + PostsRoute:** Add `PATCH /:postId` with `protect`, `userVerifiedValidation`, `postIdValidation('postId','params')`, new `patchPostValidation` (schema for body requiring both fields), then handler. Only **owner** may PATCH (403 clear message if not owner — use or add constant in `message.constant.ts` per D-07 pattern used in plan `03-03`).

    **POST /** create: replace `optionalAuth(userVerifiedValidation)` with **`protect`** then `userVerifiedValidation` (current optional chain skips verification when no Bearer and relies on `getUserId` failing later — use explicit `protect` for mutations).

    **Replace all in-repo references** `EPostAudience.FOLLOWERS` → `EPostAudience.FRIENDS_ONLY` and old string literals in this plan’s files only; wider repo sweep is `03-04` / `03-02` where needed.

    **GitNexus:** Before editing symbols, run impact analysis per `CLAUDE.md` / `AGENTS.md` for touched exported symbols (`EPostAudience`, `IPost`, `PostsService`, etc.).
  </action>
  <verify>
    <automated>cd /Users/dolong/Documents/NodeJS/nodejs-social && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "patchPost|PATCH" src/routes/posts.route.ts src/controllers/posts.controller.ts` shows wired PATCH handler.
    - `rg "allowStrangerComments" src/validations/posts.validation.ts src/repositories/post.repository.ts src/services/posts.service.ts` matches in each file.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Create and PATCH post paths persist new audience literals and `allowStrangerComments`; POST-03 satisfied for author updates.</done>
</task>

</tasks>

## verification_criteria

- `npm run build` succeeds.
- No new documents written with `followers` or `only_me` audience strings from this API path.

## success_criteria

- POST-01, POST-02, POST-03 are implementable and enforced at validation + persistence + PATCH for author.

<output>
After completion, create `.planning/phases/03-posts-feed-engagement/03-01-SUMMARY.md`
</output>
