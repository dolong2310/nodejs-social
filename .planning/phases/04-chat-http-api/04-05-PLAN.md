---
phase: 04-chat-http-api
plan: "05"
type: execute
wave: 5
depends_on: ["04-04"]
files_modified:
  - src/services/chatMessages.service.ts
  - src/services/media.service.ts
  - src/services/s3.service.ts
  - src/routes/chats.route.ts
  - src/app.ts
  - src/container/index.ts
  - scripts/fake-data.ts
  - src/utils/swaggerPaths/*.ts
  - src/database/mongodb/database.service.ts
  - src/repositories/conversation.repository.ts
  - src/services/conversations.service.ts
  - src/controllers/conversations.controller.ts
  - src/routes/conversations.route.ts
autonomous: true
requirements:
  - CHAT-04
  - CHAT-03

must_haves:
  truths:
    - **CHAT-04:** upload/presign **S3** giống post; **tối đa 5MB mỗi file** (D-14); **ảnh + file**; từ chối vượt limit trước upload.
    - **Gỡ** stack thử nghiệm: social `conversations` **repository/service/controller/route** cũ và **mount** cũ; **gỡ** `createConversationsIndex` + getter `conversations` khỏi `DatabaseService` **sau khi** không còn reference (hoặc để collection orphan — document cleanup DB thủ công).
    - OpenAPI + `fake-data` phản ánh API chat mới (tối thiểu paths chính).
  artifacts:
    - path: src/routes/chats.route.ts
      provides: attachment flow wired to message create (hoặc 2-step: presign then POST message with keys)
    - path: scripts/fake-data.ts
      provides: seed chats/messages nếu có trong script
  key_links:
    - from: MediaService / S3
      to: Chat message attachments
      via: shared upload pattern
---

## Objective

Hoàn thiện **đính kèm chat** (S3, 5MB/file), **dọn** code conversations thử nghiệm trên social DB, **Swagger + fake-data** cho Phase 4.

## Scope

- In: extend POST message (hoặc dedicated upload endpoints) với attachment metadata; reuse `media`/`s3` patterns; remove `ConversationsRoute`/`ConversationService`/old wiring; update `app.ts` chỉ mount router mới; prune `DatabaseService` conversations index method; swagger paths; fake-data.
- Out: Socket, notifications.

## Attachment flow (discretion)

Chọn một: (a) presign URL giống post rồi message body chứa `attachmentKeys[]`, hoặc (b) multipart trực tiếp — ưu tiên **reuse post** để ít rủi ro.

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-chat-http-api/04-CONTEXT.md
@src/services/media.service.ts
@src/services/s3.service.ts
@src/routes/media.route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: CHAT-04 — 5MB/file + message payload attachments</name>
  <files>src/services/chatMessages.service.ts, src/validations/chats.validation.ts, src/routes/chats.route.ts</files>
  <read_first>src/services/media.service.ts</read_first>
  <action>
    Validate size **per file** 5MB. Lưu trên message: mime, size, s3 key/url theo schema đã định 04-02. Không Cloudinary.
  </action>
  <verify>
    <automated>cd /Users/dolong/Documents/NodeJS/nodejs-social && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File > 5MB rejected với lỗi rõ.
  </acceptance_criteria>
  <done>D-14 satisfied cho HTTP.</done>
</task>

<task type="auto">
  <name>Task 2: Remove experimental conversations stack + social DB wiring</name>
  <files>src/app.ts, src/container/index.ts, src/database/mongodb/database.service.ts, + delete or gut old conversation files</files>
  <read_first>src/container/index.ts, grep conversations</read_first>
  <action>
    `rg conversations` — đảm bảo không còn import dead. Xóa hoặc deprecate file: `conversations.route.ts`, `conversations.controller.ts`, `conversations.service.ts`, `conversation.repository.ts`, `conversation.schema.ts` nếu không dùng. Gỡ `createConversationsIndex` từ `initializeIndexes`, gỡ getter `conversations` nếu an toàn.

    Cập nhật `socket.service.ts` chỉ tối thiểu: không reference removed types — nếu socket vẫn import conversation service, stub TODO cho Phase 6 (document).
  </action>
  <verify>
    <automated>cd /Users/dolong/Documents/NodeJS/nodejs-social && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Build passes.
    - `/api/conversations` cũ không còn behavior thử nghiệm (thay bằng router mới hoặc đổi path — document trong SUMMARY).
  </acceptance_criteria>
  <done>Experimental flow replaced.</done>
</task>

<task type="auto">
  <name>Task 3: Swagger + fake-data</name>
  <files>scripts/fake-data.ts, src/utils/swaggerPaths/*.ts (hoặc nơi định nghĩa openapi)</files>
  <read_first>scripts/fake-data.ts</read_first>
  <action>
    Thêm/chỉnh paths cho chats/messages/read/attachments. Fake-data: tạo friends + vài chat + message nếu khả thi.
  </action>
  <verify>
    <automated>cd /Users/dolong/Documents/NodeJS/nodejs-social && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `/api/docs` không broken (manual smoke).
  </acceptance_criteria>
  <done>Phase 4 discoverable cho client.</done>
</task>

</tasks>

## Done when

SUMMARY wave 5; CONCERNS socket note còn lại được ghi rõ cho Phase 6.
