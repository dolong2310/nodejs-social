---
phase: 04-chat-http-api
plan: "04"
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - src/repositories/chatMessage.repository.ts
  - src/services/chatMessages.service.ts
  - src/controllers/chatMessages.controller.ts
  - src/routes/chats.route.ts
  - src/validations/chats.validation.ts
  - src/dtos/requests/chatMessage.request.dto.ts
  - src/dtos/responses/chatMessage.response.dto.ts
  - src/container/index.ts
autonomous: true
requirements:
  - CHAT-01
  - CHAT-03

must_haves:
  truths:
    - **Gửi tin:** `senderId` lấy **chỉ** từ JWT / `req.user`, **không** tin field client — align **CONCERNS.md**.
    - **CHAT-01 + block D-08:** nếu không phải member chat hoặc **block either way** với đối phương (direct) hoặc policy group → **403**; **không persist** tin vi phạm.
    - **D-09:** phía bị block vẫn **đọc được** lịch sử (GET messages) nếu vẫn là member; không cho POST mới khi D-08.
    - **D-12:** `PATCH .../read` (hoặc tên tương đương) cập nhật `lastReadMessageId` / `lastReadAt` trên `chatMembers` cho **current user**.
    - **D-13:** `GET .../messages` cursor-based (`limit` + `cursor`), sort `createdAt` desc.
  artifacts:
    - path: src/services/chatMessages.service.ts
      provides: send + list + markRead + membership guards
  key_links:
    - from: ChatMessagesService
      to: BlockRepository, ChatMemberRepository
      via: send + list guards
---

## Objective

**Tin nhắn** HTTP: gửi (text-only trong wave này nếu attachment chờ 04-05), lịch sử cursor, mark read; **sender** server-authoritative.

## Scope

- In: `ChatMessageRepository`, `ChatMessageService`, controller methods, routes mounted under cùng prefix với plan 04-03, validation, DTO, container.
- Out: S3 upload attachment payload (04-05); Socket emit.

## REST sketch

- `POST /api/.../:conversationId/messages` — `{ text: string }` (attachments optional stub null).
- `GET /api/.../:conversationId/messages?limit=&cursor=` — trả page + `nextCursor`.
- `PATCH /api/.../:conversationId/read` — `{ lastReadMessageId }` hoặc server tự set theo “last message id hiện có” — document; khớp **mark read rõ ràng** (D-12).

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-chat-http-api/04-CONTEXT.md
@.planning/codebase/CONCERNS.md
@src/middlewares/auth.middleware.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: ChatMessageRepository + service core</name>
  <files>src/repositories/chatMessage.repository.ts, src/services/chatMessages.service.ts</files>
  <read_first>src/repositories/base.repository.ts</read_first>
  <action>
    `insertMessage`, `findMessagesByChatBeforeCursor(conversationId, limit, cursor)`, `updateMemberReadState`. Cập nhật `chats.updatedAt` hoặc denormalized `lastMessagePreview` nếu cần cho list (Claude discretion — ghi trong SUMMARY).
  </action>
  <verify>
    <automated>cd /Users/dolong/Documents/NodeJS/nodejs-social && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Cursor stable khi sort desc (tie-break `_id`).
  </acceptance_criteria>
  <done>Persistence messages hoàn chỉnh.</done>
</task>

<task type="auto">
  <name>Task 2: HTTP — POST/GET messages + PATCH read + guards</name>
  <files>src/controllers/chatMessages.controller.ts, src/routes/chats.route.ts, src/validations/chats.validation.ts, src/container/index.ts</files>
  <read_first>src/controllers/posts.controller.ts</read_first>
  <action>
    Mọi mutation: verify membership + friends/block rules. Log hoặc silent 403 cho block send.

    **Verified users:** nếu project đã dùng `userVerifiedValidation` cho conversations cũ — giữ hoặc bỏ nhất quán (document).
  </action>
  <verify>
    <automated>cd /Users/dolong/Documents/NodeJS/nodejs-social && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Hai friends: gửi + đọc history + mark read thành công.
    - Block: không gửi được; vẫn GET history (D-09) nếu product cho phép member record tồn tại.
  </acceptance_criteria>
  <done>CHAT-03 core HTTP hoàn thành (phần message + read).</done>
</task>

</tasks>

## Done when

SUMMARY wave 4; manual test flow friends + block send denial.
