---
phase: 07-quality-hardening-tests
plan: "03"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - .planning/phases/07-quality-hardening-tests/07-VERIFICATION.md
  - .planning/codebase/CONCERNS.md
autonomous: true
requirements:
  - QUAL-01
user_setup: []
must_haves:
  truths:
    - "`07-VERIFICATION.md` lists checklist items for socket identity + persist-then-emit on **implemented** chat/notification paths with code references + test file paths (D-05)."
    - "`CONCERNS.md` gains a **Resolved / verified** (or status) section pointing to Phase 7 + `07-VERIFICATION.md` without deleting the 2026-03-21 audit rows (D-05)."
    - "Obsolete evidence paths (`conversations.route.ts`, client `senderId` socket send) are explicitly marked superseded vs current `src/routes/chats.route.ts` + `src/services/socket.service.ts` + HTTP message controllers (per 07-RESEARCH.md reconciliation)."
  artifacts:
    - path: .planning/phases/07-quality-hardening-tests/07-VERIFICATION.md
      provides: "Checklist + grep commands + traceability to tests"
      min_lines: 40
    - path: .planning/codebase/CONCERNS.md
      provides: "Resolved/verified table for QUAL-01 scope"
      min_lines: 1
  key_links:
    - from: ".planning/codebase/CONCERNS.md"
      to: ".planning/phases/07-quality-hardening-tests/07-VERIFICATION.md"
      via: "explicit markdown link"
    - from: "07-VERIFICATION.md"
      to: "tests/integration/smoke.integration.test.ts"
      via: "HTTP persist path evidence"
    - from: "07-VERIFICATION.md"
      to: "tests/integration/socket.chat-subscribe.test.ts"
      via: "JWT + chat:subscribe evidence"
---

<objective>
Produce QUAL-01 evidence: a phase verification checklist and update `CONCERNS.md` to mark socket-identity and persist/emit concerns **verified closed or superseded** on the current shipped paths, per D-05 and phase boundary (no P2 S3/`any`/counters work).

Purpose: Close the documentation loop for auditors and future refactors without expanding scope beyond QUAL-01.

Output: New or filled `.planning/phases/07-quality-hardening-tests/07-VERIFICATION.md` and an additive **Resolved / verified** section in `.planning/codebase/CONCERNS.md`.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/codebase/CONCERNS.md
@.planning/phases/07-quality-hardening-tests/07-RESEARCH.md
@.planning/phases/07-quality-hardening-tests/07-CONTEXT.md
@src/services/socket.service.ts
@src/constants/socket.constant.ts
@src/controllers/chatMessages.controller.ts
@src/services/chatMessages.service.ts
@tests/integration/smoke.integration.test.ts
@tests/integration/socket.chat-subscribe.test.ts

<interfaces>
Useful grep anchors (executor runs and records output snippets / pass-fail in `07-VERIFICATION.md`):
- `rg "senderId" src/services/socket.service.ts`
- `rg "sendMessage" src/services/socket.service.ts`
- `rg "emitMessageCreated|emitReadUpdated" src/services`
- `rg "IRealtimeChatEmitter" src`
- `rg "persistAndEmit|emitToRecipient" src/services/notifications.service.ts`

Persist-then-emit implementation reference: `SocketService` implements `IRealtimeChatEmitter`; chat message HTTP path should call emitter **after** DB write (verify in `chatMessages.service.ts` or equivalent). Notifications: `NotificationsService` private `persistAndEmit` must **await** `notificationRepository.insertOne` (and trim) **before** `emitToRecipient` → `NOTIFICATION_SOCKET_EVENT` (`notification:new`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 07-VERIFICATION.md checklist + command transcript</name>
  <read_first>.planning/phases/07-quality-hardening-tests/07-RESEARCH.md § CONCERNS.md reconciliation; .planning/phases/06-realtime-socket-io/06-VERIFICATION.md (style reference if present)</read_first>
  <files>.planning/phases/07-quality-hardening-tests/07-VERIFICATION.md</files>
  <action>
    Create `.planning/phases/07-quality-hardening-tests/07-VERIFICATION.md` with YAML frontmatter `phase: 07-quality-hardening-tests`, `status: complete` (or `verified` when executor finishes), date, and sections:
    1. **Scope** — QUAL-01 only: socket identity + persist/emit on **implemented** chat + notification paths; explicitly **out of scope**: P2/P3 items from CONCERNS (per CONTEXT deferred).
    2. **Socket identity** — Checklist: JWT from `socket.handshake.auth.Authorization`; access token revalidated on packets; `userId` from decoded token for subscribe/typing; **no** chat message send handler trusting client `senderId` on `socket.service.ts`. For each item: **Evidence** = file:line or `rg` command used + short result (“no match” / snippet).
    3. **Persist-then-emit** — Two sub-parts, both required:
       - **3a Chat** — Messages and read updates emitted only via `IRealtimeChatEmitter` after HTTP/service persistence (cite `ChatMessagesService` / controller flow); socket path does not claim to persist DMs.
       - **3b Notifications (NOTF-03 / roadmap)** — Checklist: notification rows persisted before socket fan-out. Evidence: `src/services/notifications.service.ts` method `persistAndEmit` (lines ~87–91): `await this.notificationRepository.insertOne(doc)` and `await this.trimIfNeeded(...)` **before** `this.emitToRecipient(...)` which uses `NOTIFICATION_SOCKET_EVENT` (`notification:new`). Record `rg "persistAndEmit" src/services/notifications.service.ts` output or file:line in the doc.
    4. **Automated tests** — Table linking `tests/integration/smoke.integration.test.ts` (HTTP chat write/read) and `tests/integration/socket.chat-subscribe.test.ts` (D-03) to checklist rows.
    5. **Obsolete audit rows** — Explicit note that CONCERNS matrix references to `src/routes/conversations.route.ts` / socket `sendMessage` persistence TODOs are **historical**; superseded by Phase 4–6 architecture (reference `07-RESEARCH.md` table).
    6. **Local DoD** — `npm test` (D-06); **CI deferred** (D-07).
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/07-quality-hardening-tests/07-VERIFICATION.md` exits 0.
    - `grep -q "QUAL-01" .planning/phases/07-quality-hardening-tests/07-VERIFICATION.md` exits 0.
    - `grep -q "smoke.integration.test" .planning/phases/07-quality-hardening-tests/07-VERIFICATION.md` exits 0.
    - `grep -q "socket.chat-subscribe" .planning/phases/07-quality-hardening-tests/07-VERIFICATION.md` exits 0.
    - `grep -q "D-07" .planning/phases/07-quality-hardening-tests/07-VERIFICATION.md` exits 0 (and document CI deferred per D-07 in prose).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/dolong/Documents/NodeJS/nodejs-social &amp;&amp; test -s .planning/phases/07-quality-hardening-tests/07-VERIFICATION.md &amp;&amp; grep -q QUAL-01 .planning/phases/07-quality-hardening-tests/07-VERIFICATION.md</automated>
  </verify>
  <done>D-05 layer (1) complete: standalone verification doc with traceability to code + tests.</done>
</task>

<task type="auto">
  <name>Task 2: Update CONCERNS.md — Resolved / verified table + links</name>
  <read_first>.planning/codebase/CONCERNS.md full file; .planning/phases/07-quality-hardening-tests/07-VERIFICATION.md (after task 1)</read_first>
  <files>.planning/codebase/CONCERNS.md</files>
  <action>
    1. **Do not delete** the 2026-03-21 matrix or narrative; append a clearly separated section **Resolved / verified (Phase 7 — QUAL-01)** dated 2026-03-23 or execution date.
    2. Add a markdown table with columns: **Concern (reference P0/P1 row title)**, **Status** (`verified closed` | `superseded` | `unchanged — out of QUAL-01 scope`), **Phase / date**, **Evidence** (link to `.planning/phases/07-quality-hardening-tests/07-VERIFICATION.md` and/or relative path to test files).
    3. Minimum rows to address:
       - P0 missing test suite → link `npm test` + integration test paths.
       - P1 socket bypasses persistence (old conversations evidence) → **superseded**; point to `/api/chats` + HTTP-first + emitter.
       - Tech debt socket `sendMessage` TODO / Known bug client `senderId` → **superseded or verified absent** with `rg` evidence recorded in 07-VERIFICATION.
       - Socket auth hardening (derive identity from JWT) → **verified** with D-03 test reference.
       - P1 boot single-point failure → **unchanged / out of QUAL-01 narrow scope** (explicit per CONTEXT).
    4. Include one line: **CI automation deferred** per D-07 (local `npm test` only for this phase).
  </action>
  <acceptance_criteria>
    - `grep -q "07-VERIFICATION.md" .planning/codebase/CONCERNS.md` exits 0.
    - `grep -q "Phase 7" .planning/codebase/CONCERNS.md` exits 0.
    - `grep -q "superseded\|verified" .planning/codebase/CONCERNS.md` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/dolong/Documents/NodeJS/nodejs-social &amp;&amp; grep -q "07-VERIFICATION.md" .planning/codebase/CONCERNS.md &amp;&amp; npm test</automated>
  </verify>
  <done>QUAL-01 documentation gate complete; CONCERNS reflects current architecture vs stale evidence (D-05 layer 2).</done>
</task>

</tasks>

<verification>
Human-readable audit trail: `07-VERIFICATION.md` + updated `CONCERNS.md`; `npm test` still green.
</verification>

<success_criteria>
- Every QUAL-01 concern in roadmap language is either verified, superseded with replacement pointers, or explicitly deferred with reason.
- No REQUIREMENTS.md edits required in this plan (optional executor note if trace table desired).
</success_criteria>

<output>
After completion, create `.planning/phases/07-quality-hardening-tests/07-03-SUMMARY.md`
</output>
