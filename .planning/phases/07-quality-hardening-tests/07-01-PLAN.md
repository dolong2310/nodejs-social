---
phase: 07-quality-hardening-tests
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - tests/setup/register-argv.mjs
  - tests/README.md
  - tests/helpers/integration-app.ts
  - tests/integration/smoke.integration.test.ts
autonomous: true
requirements:
  - QUAL-02
user_setup:
  - service: mongodb
    why: "Integration smoke uses real social + chat DBs from DATABASE_URI"
    env_vars:
      - name: DATABASE_URI
        source: "Local MongoDB connection string (same as dev)"
      - name: DATABASE_NAME
        source: "Dedicated test DB name, e.g. nodejs_social_test (override in shell or .env.development)"
      - name: DATABASE_CHAT_NAME
        source: "Dedicated chat DB name, e.g. nodejs_social_chat_test"
  - service: redis
    why: "createApp initializes Redis + rate-limit store + BullMQ"
    env_vars:
      - name: REDIS_HOST
        source: "Local Redis"
      - name: REDIS_PORT
      - name: REDIS_PASSWORD
      - name: REDIS_DB
        source: "Use non-dev index (e.g. 15) to avoid key collisions"
must_haves:
  truths:
    - "`npm test` exits 0 on a dev machine with Mongo + Redis up and env satisfied (D-06)."
    - "One integration file proves register → DB token → verify-email → Bearer `GET /api/users/me` (D-01a)."
    - "Same flow proves `POST /api/friends/requests` + `POST /api/friends/requests/:fromUserId/accept` between two verified users (D-01b)."
    - "Same flow proves `POST /api/chats/direct` + `POST /api/chats/:chatId/messages` + `GET /api/chats/:chatId/messages` text-only (D-01c)."
  artifacts:
    - path: vitest.config.ts
      provides: "Vitest 4 + `@/*` alias + serial integration pool"
      min_lines: 20
    - path: tests/integration/smoke.integration.test.ts
      provides: "End-to-end HTTP smoke"
      min_lines: 80
    - path: tests/helpers/integration-app.ts
      provides: "createServer + createApp + listen(0) + teardown"
      min_lines: 40
  key_links:
    - from: "tests/integration/smoke.integration.test.ts"
      to: "POST /api/auth/register"
      via: "supertest"
    - from: "tests/integration/smoke.integration.test.ts"
      to: "MongoDB users.emailVerificationToken"
      via: "DatabaseInstance.get().users.findOne"
    - from: "tests/integration/smoke.integration.test.ts"
      to: "POST /api/auth/verify-email"
      via: "body.token"
---

<objective>
Add Vitest + supertest, a Vitest runner entry that sets `--env=development` for `envConfig` (`src/config/envConfig.ts`), and one integration smoke test covering auth (with verify-email), friends (request + accept), and chat HTTP (direct + text message + list) per D-01/D-02 and QUAL-02.

Purpose: Establish the automated baseline and local DoD entrypoint (`npm test`) before socket tests and CONCERNS documentation (D-06).

Output: Runnable `npm test`, `vitest.config.ts`, `tests/integration/smoke.integration.test.ts`, env/runbook in `tests/README.md`.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-quality-hardening-tests/07-CONTEXT.md
@.planning/phases/07-quality-hardening-tests/07-RESEARCH.md
@src/app.ts
@src/index.ts
@src/config/envConfig.ts
@src/config/generalConfig.ts
@package.json

<interfaces>
From `src/config/generalConfig.ts`:
```typescript
export const config = {
  api: { prefix: '/api' },
  // ... database, redis, rateLimit from envConfig
};
```

From `src/app.ts`:
```typescript
export async function createApp(httpServer: HttpServer, appConfig: AppConfig): Promise<Express>;
```

HTTP surface (prefix `/api`):
- Auth: `POST /api/auth/register`, `POST /api/auth/login`, `POST /api/auth/verify-email` (body `{ token }`, no `protect`)
- Users: `GET /api/users/me` (`protect` only — use for Bearer smoke)
- Friends: `POST /api/friends/requests`, `POST /api/friends/requests/:fromUserId/accept`
- Chats: `POST /api/chats/direct` (body `{ peerUserId }`), `POST /api/chats/:chatId/messages`, `GET /api/chats/:chatId/messages`

From `src/database/mongodb/database.service.ts`:
- `DatabaseInstance.get().users` — `Collection<IUser>` for reading `emailVerificationToken` after register

Register body (validated): `name`, `email`, `password`, `confirmPassword`, `dateOfBirth` (ISO8601 strict, e.g. `2000-01-01T00:00:00.000Z`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vitest toolchain + argv shim + npm test script</name>
  <read_first>src/config/envConfig.ts (lines 1–18: minimist `env` → dotenv path); package.json scripts; .planning/phases/07-quality-hardening-tests/07-RESEARCH.md § Common Pitfalls pitfall 1.</read_first>
  <files>package.json, vitest.config.ts, tests/setup/register-argv.mjs, tests/README.md</files>
  <action>
    1. `npm install -D vitest@^4.1.0 supertest@^7.2.2 @types/supertest@^7.2.0` (versions per 07-RESEARCH.md; adjust patch if npm resolves newer minors).
    2. Add `tests/setup/register-argv.mjs` with **only** CommonJS or ESM code that runs as Node `--import` preload: first line of logic must be `process.argv.push('--env', 'development');` so `envConfig` loads `.env.development` when test workers import `@/config` (mirrors `nodemon --env=development`).
    3. Set `package.json` script **`"test"`** to: `node --import ./tests/setup/register-argv.mjs ./node_modules/vitest/vitest.mjs run` (if `vitest.mjs` path differs in installed package, use the documented entry from `node_modules/vitest` — executor adjusts path but keeps `node --import ./tests/setup/register-argv.mjs` prefix per D-06).
    4. Add `vitest.config.ts` at repo root using `defineConfig` from `vitest/config`: `resolve.alias['@']` → `path.resolve(__dirname, './src')` (match `tsconfig.json` paths); `test.environment = 'node'`; `test.include = ['tests/integration/**/*.test.ts']`; **`test.fileParallelism = false`**; **`test.poolOptions.forks.singleFork = true`** (or equivalent serial config per 07-RESEARCH.md); `test.testTimeout` and `test.hookTimeout` ≥ `60000`.
    5. Write `tests/README.md`: list prerequisites (MongoDB + Redis running); require a **complete** `.env.development` (all keys in `ENV_KEYS` from `envConfig.ts` non-empty); recommend overrides for isolation: `DATABASE_NAME=nodejs_social_test`, `DATABASE_CHAT_NAME=nodejs_social_chat_test`, `REDIS_DB=15`, `FRONTEND_URL=http://127.0.0.1` (needed for Phase 7 socket tests in plan 02); set `RATE_LIMIT_ENABLED=0` unless you intentionally test limits; state **CI/GitHub Actions is out of scope for Phase 7 DoD** (D-07).
  </action>
  <acceptance_criteria>
    - `grep -q register-argv.mjs package.json` exits 0.
    - `grep -q "alias" vitest.config.ts && grep -q tests/integration vitest.config.ts` exits 0.
    - `test -f tests/setup/register-argv.mjs` exits 0.
    - `grep -q DATABASE_CHAT_NAME tests/README.md` exits 0; `grep -q FRONTEND_URL tests/README.md` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/dolong/Documents/NodeJS/nodejs-social &amp;&amp; node --import ./tests/setup/register-argv.mjs ./node_modules/vitest/vitest.mjs run --passWithNoTests</automated>
  </verify>
  <done>Vitest runs with preload (may pass with no tests yet); README documents env keys and local-only DoD.</done>
</task>

<task type="auto">
  <name>Task 2: Integration harness + smoke.integration.test.ts (auth + friends + chat HTTP)</name>
  <read_first>src/index.ts (createServer + createApp appConfig shape); src/app.ts; src/routes/auth.route.ts; src/routes/friends.route.ts; src/routes/chats.route.ts; src/routes/users.route.ts</read_first>
  <files>tests/helpers/integration-app.ts, tests/integration/smoke.integration.test.ts</files>
  <action>
    1. Implement `tests/helpers/integration-app.ts` exporting async `startIntegrationHttpServer()` that: creates `http.Server` via `createServer()` from `node:http`; calls `await createApp(server, { database: { uri: config.database.uri, databaseName: config.database.name, chatDatabaseName: envConfig.DATABASE_CHAT_NAME }, redis: config.redis, cors: { origin: envConfig.FRONTEND_URL, credentials: true }, rateLimitOptions: { windowMs: config.rateLimit.windowMs, limit: config.rateLimit.limit, standardHeaders: config.rateLimit.standardHeaders, legacyHeaders: config.rateLimit.legacyHeaders, ipv6Subnet: config.rateLimit.ipv6Subnet } })` importing `createApp` from `@/app`, `config` from `@/config`, `envConfig` from `@/config/envConfig` — **mirror `src/index.ts` but use `envConfig.FRONTEND_URL` for cors.origin** (not `isProduction ? … : '*'`) so Socket.IO CORS in plan 02 stays consistent; listens on `127.0.0.1` port `0`; returns `{ baseUrl: \`http://127.0.0.1:${port}\`, address, close: () => Promise that closes http server and calls `DatabaseInstance.get().close()`, `RedisInstance.get().disconnect()`, `QueueService.close()` like graceful shutdown in `src/app.ts`.
    2. In `tests/integration/smoke.integration.test.ts`, one `describe` with `beforeAll` starting server once and `afterAll` closing; use **supertest** bound to the **`http.Server`** instance (same pattern as 07-RESEARCH.md: `supertest(server)`), base path **`/api`**.
    3. **User A / User B** with unique emails `smoke-a-${Date.now()}@test.local` / `smoke-b-${Date.now()}@test.local` (or similar); password `Smoke-test-1` for both; register body includes valid `confirmPassword` and ISO8601 `dateOfBirth`.
    4. After each `POST /api/auth/register`, read `emailVerificationToken` from `DatabaseInstance.get().users.findOne({ email })` projection `{ emailVerificationToken: 1 }`; `POST /api/auth/verify-email` with JSON `{ "token": "&lt;value from DB&gt;" }` until status 200 for each user.
    5. `POST /api/auth/login` each user; store `accessToken` from JSON body (follow actual API envelope: project `sendResponse` shape — assert `response.body.data.accessToken` or adjust to match `LoginResponseDTO` field names used in controllers).
    6. **Bearer (D-01a):** `GET /api/users/me` with `Authorization: Bearer &lt;accessTokenA&gt;` → expect 200.
    7. **Friends (D-01b):** A sends `POST /api/friends/requests` with JSON `{ "toUserId": "&lt;hex id of B&gt;" }` (`sendRequestToUserIdValidation` in `src/validations/friends.validation.ts`); B accepts with `POST /api/friends/requests/${userIdA}/accept` with B’s Bearer token.
    8. **Chat HTTP (D-01c):** A `POST /api/chats/direct` with `{ "peerUserId": "&lt;hex id of B&gt;" }` → parse `data.id` as `chatId`; A `POST /api/chats/${chatId}/messages` with `{ "text": "smoke hello" }` → expect 201/200 per API; A `GET /api/chats/${chatId}/messages` → expect listed message contains the text.
    9. Do **not** add S3 attachment assertions (D-01); keep smoke minimal (D-02).
  </action>
  <acceptance_criteria>
    - `rg "POST /api/chats/direct" tests/integration/smoke.integration.test.ts` exits 0.
    - `rg "emailVerificationToken" tests/integration/smoke.integration.test.ts` exits 0.
    - `rg "friends/requests" tests/integration/smoke.integration.test.ts` exits 0.
    - `npm test` exits 0 when Mongo + Redis are up and `.env.development` satisfies `ENV_KEYS`.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/dolong/Documents/NodeJS/nodejs-social &amp;&amp; npm test</automated>
  </verify>
  <done>QUAL-02 smoke path automated: auth + verified users + friends + chat HTTP text-only; `npm test` is the local gate (D-06).</done>
</task>

</tasks>

<verification>
Full `npm test` green with local Mongo/Redis; no new CI workflow files (D-07).
</verification>

<success_criteria>
- `npm test` runs Vitest with `--env=development` behavior via preload.
- Single integration test file covers D-01a–c without S3.
</success_criteria>

<output>
After completion, create `.planning/phases/07-quality-hardening-tests/07-01-SUMMARY.md`
</output>
