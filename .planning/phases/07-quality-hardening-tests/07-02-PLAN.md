---
phase: 07-quality-hardening-tests
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - tests/integration/socket.chat-subscribe.test.ts
  - tests/helpers/integration-app.ts
autonomous: true
requirements:
  - QUAL-02
user_setup:
  - service: mongodb
    why: "Reuses same integration stack as smoke test"
    env_vars:
      - name: DATABASE_URI
      - name: DATABASE_NAME
      - name: DATABASE_CHAT_NAME
  - service: redis
    why: "createApp requires Redis"
    env_vars:
      - name: REDIS_HOST
      - name: REDIS_PORT
      - name: REDIS_PASSWORD
      - name: REDIS_DB
must_haves:
  truths:
    - "Socket.IO client connects using `auth: { Authorization: 'Bearer &lt;access&gt;' }` matching `SocketService.initMiddleware` (D-03, SOCK-01 alignment)."
    - "After HTTP setup (two verified friends + direct chat), client emits `chat:subscribe` with `{ conversationId: &lt;conversationId&gt; }` (`SOCKET_CLIENT_CHAT_SUBSCRIBE`); server joins room `chat:&lt;id&gt;` (`chatRoom()` from `src/constants/socket.constant.ts`) for members only (D-03, SOCK-02)."
    - "Observable signal: listener receives `presence:chat` (`SOCKET_SERVER_PRESENCE_CHAT`) for that conversation after subscribe (server calls `emitChatPresenceAggregate` after join — use this instead of requiring `chat:message:new`, D-04 optional stretch remains optional)."
  artifacts:
    - path: tests/integration/socket.chat-subscribe.test.ts
      provides: "Socket JWT handshake + chat:subscribe membership path"
      min_lines: 60
  key_links:
    - from: "tests/integration/socket.chat-subscribe.test.ts"
      to: "src/services/socket.service.ts"
      via: "handshake auth + SOCKET_CLIENT_CHAT_SUBSCRIBE handler"
    - from: "tests/integration/socket.chat-subscribe.test.ts"
      to: "src/constants/socket.constant.ts"
      via: "event names `chat:subscribe`, `presence:chat`, `chatRoom` prefix `chat:`"
---

<objective>
Add one Socket.IO integration test that authenticates with a real access JWT, subscribes to the direct conversation room as a chat member, and observes a server-emitted presence signal — fulfilling D-03/D-04 discretion and aligning with SOCK-01/SOCK-02 without mandating full event matrix coverage.

Purpose: Lock realtime join semantics to the same HTTP-created conversation as smoke tests.

Output: `tests/integration/socket.chat-subscribe.test.ts` (and minimal helper extension if needed).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-quality-hardening-tests/07-CONTEXT.md
@src/services/socket.service.ts
@src/constants/socket.constant.ts
@tests/helpers/integration-app.ts
@tests/integration/smoke.integration.test.ts

<interfaces>
From `src/constants/socket.constant.ts`:
```typescript
export const SOCKET_CLIENT_CHAT_SUBSCRIBE = 'chat:subscribe';
export const SOCKET_SERVER_PRESENCE_CHAT = 'presence:chat';
export function chatRoom(conversationId: string): string;
```

From `src/services/socket.service.ts` (handshake):
- Reads `socket.handshake.auth.Authorization`, Bearer access token, `verifyAccessToken`, rejects unverified users.
- On `SOCKET_CLIENT_CHAT_SUBSCRIBE`, loads membership via `chatMemberRepository.findMembership`; if present, `socket.join(chatRoom(raw))` then `emitChatPresenceAggregate(raw)`.

Socket.IO server CORS: `{ origin: envConfig.FRONTEND_URL, credentials: true }` — **test env must set `FRONTEND_URL` to match the origin the client sends** (use `http://127.0.0.1:&lt;port&gt;` for both `FRONTEND_URL` in `.env.development` during tests and the `io()` URL, per 07-RESEARCH.md pitfall 5).

Client (`socket.io-client` already in devDependencies): `import { io as ioc } from 'socket.io-client'` then `ioc(baseUrl, { auth: { Authorization: \`Bearer ${accessToken}\` } })`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend integration harness if needed for Socket base URL</name>
  <read_first>tests/helpers/integration-app.ts from plan 01; src/services/socket.service.ts constructor (CORS origin)</read_first>
  <files>tests/helpers/integration-app.ts</files>
  <action>
    If the harness from 07-01 does not already expose `baseUrl` as `http://127.0.0.1:&lt;port&gt;` and a reference to the listening `http.Server`, extend it minimally so socket tests can reuse the same `startIntegrationHttpServer()` without duplicating `createApp` wiring. Do not change production `src/index.ts` CORS behavior; tests rely on `envConfig.FRONTEND_URL` matching the client URL origin behavior (document in `tests/README.md` if you add a note about setting `FRONTEND_URL` for socket tests).
  </action>
  <acceptance_criteria>
    - `rg "startIntegrationHttpServer" tests/helpers/integration-app.ts` exits 0.
    - `rg "127.0.0.1" tests/helpers/integration-app.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/dolong/Documents/NodeJS/nodejs-social &amp;&amp; npm test -- tests/integration/smoke.integration.test.ts</automated>
  </verify>
  <done>Smoke test still passes unchanged after harness tweak.</done>
</task>

<task type="auto">
  <name>Task 2: socket.chat-subscribe.test.ts — JWT connect + chat:subscribe + presence:chat</name>
  <read_first>src/services/socket.service.ts (`initMiddleware`, `SOCKET_CLIENT_CHAT_SUBSCRIBE` handler, `emitChatPresenceAggregate`); tests/integration/smoke.integration.test.ts (reuse patterns for register/verify/login/friends/direct chat)</read_first>
  <files>tests/integration/socket.chat-subscribe.test.ts</files>
  <action>
    1. New file `tests/integration/socket.chat-subscribe.test.ts` using the same **serial** integration assumptions as plan 01 (`fileParallelism` already false).
    2. `beforeAll`: start HTTP server via `startIntegrationHttpServer()` from `tests/helpers/integration-app.ts`.
    3. Build **verified** users C and D (unique emails), obtain `accessTokenC`, and create a **direct chat** between them via HTTP (`POST /api/chats/direct` as C with `peerUserId` = D) exactly like smoke; capture `conversationId` from response `data.id`.
    4. `import { io as ioc } from 'socket.io-client'`; connect to `${baseUrl}` with `auth: { Authorization: \`Bearer ${accessTokenC}\` }`; await `connect` event; **reject** on `connect_error` (log `error.message` in test output for debugging).
    5. Register one-time listener for **`presence:chat`** (`SOCKET_SERVER_PRESENCE_CHAT` from `@/constants/socket.constant.ts` — import the constant, do not string-dupe).
    6. `socket.emit(SOCKET_CLIENT_CHAT_SUBSCRIBE, { conversationId: conversationId })` using the same constant as server (`SOCKET_CLIENT_CHAT_SUBSCRIBE` / literal `chat:subscribe` must match `src/constants/socket.constant.ts`).
    7. `await` `vi.waitFor` (or Promise + timeout) until payload with `conversationId === conversationId` is received, timeout ≤ 10000ms.
    8. Disconnect socket; `afterAll` close server.
    9. **Optional (D-04 stretch, Claude discretion):** if stable in one run, add second assertion: D sends a chat message via HTTP and C listens for `SOCKET_SERVER_CHAT_MESSAGE_NEW` (`chat:message:new`) — if flaky, skip and note in test comment; do not block phase on stretch.
    10. Negative control (recommended, small): non-member user E verified, token E, connect socket, emit subscribe for same `conversationId`, **must not** receive the same membership-driven success signal as C (either no `presence:chat` for E within timeout or connection denied — choose one deterministic assertion consistent with server code paths).
  </action>
  <acceptance_criteria>
    - `rg "SOCKET_CLIENT_CHAT_SUBSCRIBE" tests/integration/socket.chat-subscribe.test.ts` exits 0.
    - `rg "presence:chat" tests/integration/socket.chat-subscribe.test.ts` exits 0; `rg "SOCKET_SERVER_PRESENCE_CHAT" tests/integration/socket.chat-subscribe.test.ts` exits 0.
    - `rg "socket.io-client" tests/integration/socket.chat-subscribe.test.ts` exits 0.
    - `npm test` exits 0 (runs full integration suite including smoke + socket file).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/dolong/Documents/NodeJS/nodejs-social &amp;&amp; npm test</automated>
  </verify>
  <done>D-03 satisfied: JWT socket auth + `chat:subscribe` on a conversation the user belongs to; QUAL-02 extended to realtime join path.</done>
</task>

</tasks>

<verification>
`npm test` includes socket test; no client-trusted `senderId` path is introduced (server already HTTP-first for messages).
</verification>

<success_criteria>
- Failing JWT or non-member cannot pass the positive `presence:chat` membership assertion.
- Event names imported from `src/constants/socket.constant.ts` (single source of truth).
</success_criteria>

<output>
After completion, create `.planning/phases/07-quality-hardening-tests/07-02-SUMMARY.md`
</output>
